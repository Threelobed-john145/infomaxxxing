import { Concept } from '@/lib/types';

export const designPatternsConcepts: Concept[] = [
  {
    id: "factory-method",
    term: "Factory Method",
    category: "design-patterns",
    tags: ["creational", "object-creation", "polymorphism"],
    oneLiner: "A parent type defines how objects get created, and subclasses decide the exact concrete type.",
    body: "Factory Method moves object creation behind a method instead of sprinkling `new` calls throughout the codebase. The caller depends on an abstraction and lets a specialized implementation choose the exact instance to return. It is useful when the right type depends on runtime context or platform-specific behavior.",
    example: "A document editor calls `createExporter()` and gets a PDF exporter on one flow and a Markdown exporter on another.",
    whyItMatters: "It reduces hard-coded construction logic and makes creation rules easier to extend without rewriting callers.",
    relatedTerms: ["abstract factory", "builder", "prototype", "polymorphism"],
  },
  {
    id: "abstract-factory",
    term: "Abstract Factory",
    category: "design-patterns",
    tags: ["creational", "families", "abstractions"],
    oneLiner: "Creates whole families of related objects without tying the code to concrete classes.",
    body: "Abstract Factory provides a single interface for creating multiple related objects that should work together. Instead of constructing each piece separately, the code asks one factory for the matching set. This keeps incompatible combinations from leaking into the system.",
    example: "A UI toolkit uses one factory to create macOS buttons, menus, and dialogs, and another to create Windows versions.",
    whyItMatters: "It helps large systems swap entire implementation families cleanly, which is hard to do when each object is chosen independently.",
    relatedTerms: ["factory method", "builder", "bridge", "hexagonal architecture"],
  },
  {
    id: "builder",
    term: "Builder",
    category: "design-patterns",
    tags: ["creational", "construction", "fluent-api"],
    oneLiner: "Builds a complex object step by step instead of forcing one giant constructor call.",
    body: "Builder separates the process of assembling an object from the final representation. It shines when an object has many optional parts, validation rules, or staged setup steps. The same sequence can also create different finished outputs.",
    example: "A query builder gradually adds filters, joins, sorting, and pagination before producing the final SQL statement.",
    whyItMatters: "It makes complex construction readable and safer than passing a long list of positional arguments or partially initialized data.",
    relatedTerms: ["factory method", "abstract factory", "prototype", "fluent interface"],
  },
  {
    id: "prototype",
    term: "Prototype",
    category: "design-patterns",
    tags: ["creational", "cloning", "object-creation"],
    oneLiner: "Creates new objects by copying an existing one instead of building from scratch.",
    body: "Prototype treats an existing object as the template for future instances. This is helpful when setup is expensive or when the configuration surface is too wide to reproduce repeatedly. The key detail is deciding whether copies should be shallow, deep, or selectively shared.",
    example: "A graphics app clones a preconfigured shape object and tweaks only the position and label for each new diagram node.",
    whyItMatters: "It can cut repetitive setup code and preserve complex defaults, but only if the copy semantics are explicit and safe.",
    relatedTerms: ["builder", "factory method", "flyweight", "copy-on-write"],
  },
  {
    id: "adapter",
    term: "Adapter",
    category: "design-patterns",
    tags: ["structural", "integration", "compatibility"],
    oneLiner: "Wraps one interface so another part of the system can use it without changing either side.",
    body: "Adapter sits between two mismatched APIs and translates calls, data shapes, or error handling. It is most useful when you cannot change the third-party library, legacy code, or shared contract on one side. The rest of the code can then depend on the cleaner interface instead of the awkward one.",
    example: "An app wraps a payment vendor SDK so the rest of the code always calls `charge(customer, amount)` regardless of provider quirks.",
    whyItMatters: "It localizes integration mess and keeps vendor-specific details from spreading through the codebase.",
    relatedTerms: ["bridge", "facade", "hexagonal architecture", "anti-corruption layer"],
  },
  {
    id: "bridge",
    term: "Bridge",
    category: "design-patterns",
    tags: ["structural", "abstraction", "composition"],
    oneLiner: "Separates an abstraction from its implementation so both can evolve independently.",
    body: "Bridge splits a concept into two dimensions that would otherwise create a class explosion. One side models the high-level abstraction, and the other side supplies interchangeable implementations through composition. This avoids multiplying subclasses for every combination.",
    example: "A notification system has alert types like billing and security, and delivery channels like email, SMS, and Slack, without needing a subclass per pair.",
    whyItMatters: "It keeps multi-axis variation manageable and prevents inheritance trees from turning into a maintenance trap.",
    relatedTerms: ["adapter", "abstract factory", "composition over inheritance", "strategy"],
  },
  {
    id: "composite",
    term: "Composite",
    category: "design-patterns",
    tags: ["structural", "tree", "hierarchy"],
    oneLiner: "Treats a group of objects and a single object through the same interface.",
    body: "Composite is designed for tree-shaped structures where leaves and containers should look alike to the caller. A folder can contain files and other folders, but code using the interface can ask any node for size, render output, or permissions. The recursive structure is the main idea.",
    example: "A UI framework renders a single button and a whole panel of nested components by calling the same `render()` contract.",
    whyItMatters: "It simplifies code that works over nested structures because callers stop caring whether they received one thing or many.",
    relatedTerms: ["iterator", "visitor", "decorator", "tree traversal"],
  },
  {
    id: "decorator",
    term: "Decorator",
    category: "design-patterns",
    tags: ["structural", "wrapping", "composition"],
    oneLiner: "Adds behavior around an object by wrapping it instead of changing the original class.",
    body: "Decorator preserves the same interface while layering extra behavior before or after the wrapped call. It works well for cross-cutting concerns like logging, caching, metrics, and authorization. Multiple decorators can be stacked to build behavior incrementally.",
    example: "A storage client gets wrapped with a retry decorator, then a metrics decorator, then a tracing decorator.",
    whyItMatters: "It keeps optional behavior modular and avoids giant classes that mix core logic with every operational concern.",
    relatedTerms: ["proxy", "composite", "facade", "middleware"],
  },
  {
    id: "facade",
    term: "Facade",
    category: "design-patterns",
    tags: ["structural", "simplicity", "api-design"],
    oneLiner: "Presents a simpler front door to a messy or highly detailed subsystem.",
    body: "Facade hides coordination across several components behind a small, task-focused interface. It does not necessarily remove the underlying complexity, but it gives most callers a safer and easier entry point. Power users can still reach the lower-level pieces when needed.",
    example: "A deployment service exposes `deployRelease()` even though it internally builds artifacts, runs checks, uploads files, and flips traffic.",
    whyItMatters: "It lowers the cost of using complicated subsystems and reduces the chance that each caller will reassemble the workflow differently.",
    relatedTerms: ["adapter", "decorator", "mediator", "service layer"],
  },
  {
    id: "flyweight",
    term: "Flyweight",
    category: "design-patterns",
    tags: ["structural", "memory", "sharing"],
    oneLiner: "Shares common immutable state across many objects to avoid wasting memory.",
    body: "Flyweight splits object state into intrinsic data that can be shared and extrinsic data that must stay unique per use. This matters when a system creates huge numbers of similar objects, such as glyphs, particles, or repeated map markers. The shared part is usually cached and reused.",
    example: "A text editor stores one shared glyph definition for the letter A and combines it with per-character position and formatting data.",
    whyItMatters: "It can dramatically reduce memory pressure and allocation churn in systems with massive numbers of repeated structures.",
    relatedTerms: ["prototype", "object pooling", "cache", "immutability"],
  },
  {
    id: "proxy",
    term: "Proxy",
    category: "design-patterns",
    tags: ["structural", "indirection", "control"],
    oneLiner: "Stands in front of another object to control access, timing, or remote communication.",
    body: "Proxy keeps the same interface as the real object but inserts control logic in the middle. It can lazy-load heavy resources, enforce permissions, cache results, or represent a remote object as if it were local. The caller talks to the proxy and does not need to know which case applies.",
    example: "An image proxy shows a lightweight placeholder until the full asset is actually needed on screen.",
    whyItMatters: "It gives you a clean way to add access control or deferred work without forcing callers to handle those details manually.",
    relatedTerms: ["decorator", "adapter", "lazy loading", "virtual proxy"],
  },
  {
    id: "chain-of-responsibility",
    term: "Chain of Responsibility",
    category: "design-patterns",
    tags: ["behavioral", "pipelines", "request-handling"],
    oneLiner: "Passes a request through a sequence of handlers until one handles it or the chain ends.",
    body: "Each handler gets a chance to inspect, modify, or stop the request before passing it along. This is useful when multiple rules may apply, but you do not want the sender to know which component will act. Web middleware stacks are a common modern example.",
    example: "An incoming API request flows through authentication, rate limiting, audit logging, and business validation handlers in order.",
    whyItMatters: "It makes request processing extensible and keeps the sender decoupled from the specific rule set.",
    relatedTerms: ["command", "decorator", "middleware", "pipeline"],
  },
  {
    id: "command",
    term: "Command",
    category: "design-patterns",
    tags: ["behavioral", "actions", "encapsulation"],
    oneLiner: "Wraps an action and its parameters into an object that can be queued, logged, or undone.",
    body: "Command turns a request into a first-class object. That means the system can store it, retry it later, send it over the network, or attach metadata like who triggered it. It is especially useful when the invoker should not know the details of how the work gets done.",
    example: "A text editor stores `InsertTextCommand` and `DeleteSelectionCommand` objects so it can support undo and redo.",
    whyItMatters: "It decouples the request from the execution and opens the door to queuing, auditing, scheduling, and undo workflows.",
    relatedTerms: ["cqrs", "chain of responsibility", "memento", "job queue"],
  },
  {
    id: "iterator",
    term: "Iterator",
    category: "design-patterns",
    tags: ["behavioral", "traversal", "collections"],
    oneLiner: "Provides a standard way to walk through a collection without exposing its internal structure.",
    body: "Iterator hides whether data comes from an array, tree, graph, stream, or paginated remote source. The caller asks for the next item and does not need to know how traversal works underneath. Different iterators can expose different traversal orders over the same data.",
    example: "A file search tool returns results lazily one at a time while it keeps scanning directories in the background.",
    whyItMatters: "It separates traversal behavior from storage details and makes collections easier to consume consistently.",
    relatedTerms: ["composite", "visitor", "generator", "lazy evaluation"],
  },
  {
    id: "mediator",
    term: "Mediator",
    category: "design-patterns",
    tags: ["behavioral", "coordination", "decoupling"],
    oneLiner: "Centralizes communication between components so they do not all depend on each other directly.",
    body: "Mediator introduces a coordinating object that handles interactions among peers. Instead of every component knowing how to talk to every other component, they all talk to the mediator. This reduces direct coupling but can create an oversized coordinator if used carelessly.",
    example: "A checkout flow uses one mediator to coordinate inventory checks, payment authorization, fraud review, and email confirmation.",
    whyItMatters: "It can untangle dense dependency webs and make interaction rules visible in one place.",
    relatedTerms: ["facade", "observer", "event-driven architecture", "orchestration"],
  },
  {
    id: "memento",
    term: "Memento",
    category: "design-patterns",
    tags: ["behavioral", "snapshots", "history"],
    oneLiner: "Captures an object’s state so it can be restored later without exposing its internals.",
    body: "Memento saves enough state to roll an object back to an earlier point in time. The originator knows how to produce and restore the snapshot, while another component manages the history. This keeps the internal structure private while still supporting time travel.",
    example: "A drawing app stores snapshots before each edit so users can undo a sequence of changes.",
    whyItMatters: "It is a clean way to support undo, rollback, and point-in-time recovery without leaking private state everywhere.",
    relatedTerms: ["command", "state", "snapshot", "event sourcing"],
  },
  {
    id: "state",
    term: "State",
    category: "design-patterns",
    tags: ["behavioral", "state-machines", "polymorphism"],
    oneLiner: "Changes an object’s behavior by swapping state-specific logic instead of piling up conditionals.",
    body: "State models each meaningful mode of an object as its own behavior unit. Rather than scattering `if status === ...` checks across methods, the object delegates behavior to the current state. Transitions move it to a new state object when rules require it.",
    example: "An order acts differently while it is pending, paid, shipped, or canceled, and each state controls what transitions are allowed.",
    whyItMatters: "It makes state-heavy workflows easier to reason about and keeps transition rules from collapsing into tangled conditionals.",
    relatedTerms: ["memento", "template method", "finite state machine", "saga pattern"],
  },
  {
    id: "template-method",
    term: "Template Method",
    category: "design-patterns",
    tags: ["behavioral", "inheritance", "workflow"],
    oneLiner: "Defines the fixed skeleton of an algorithm and lets subclasses fill in selected steps.",
    body: "Template Method is useful when several workflows share the same overall sequence but differ in a few operations. The parent type owns the stable order, and subclasses override hooks for the variable pieces. This preserves consistency without copying the full algorithm.",
    example: "A data import base class handles validation, parsing, persistence, and reporting, while CSV and JSON subclasses customize the parsing step.",
    whyItMatters: "It removes duplicated workflow code and enforces the same high-level process across variants.",
    relatedTerms: ["strategy", "state", "inheritance", "hook method"],
  },
  {
    id: "visitor",
    term: "Visitor",
    category: "design-patterns",
    tags: ["behavioral", "operations", "object-structure"],
    oneLiner: "Adds new operations over an object structure without changing the element classes themselves.",
    body: "Visitor separates the data structure from the operations performed on it. Each element accepts a visitor, and the visitor implements behavior for each concrete type. It works best when the set of element types is stable but the set of operations keeps growing.",
    example: "A compiler walks an abstract syntax tree with visitors for type checking, optimization, and code generation.",
    whyItMatters: "It keeps domain structures stable while allowing new analyses or transformations to be added without editing every element class.",
    relatedTerms: ["composite", "iterator", "double dispatch", "abstract syntax tree"],
  },
  {
    id: "repository-pattern",
    term: "Repository Pattern",
    category: "design-patterns",
    tags: ["persistence", "domain-model", "abstraction"],
    oneLiner: "Presents stored data through collection-like operations so domain code is not tied to database details.",
    body: "A repository gives the application a domain-oriented way to load and save objects. Instead of spreading SQL, ORM queries, or API calls across the codebase, persistence logic lives behind a focused interface. The goal is to make the domain talk about orders, invoices, or accounts, not tables and joins.",
    example: "Checkout code calls `orderRepository.findPendingById(id)` instead of embedding raw query logic in the service.",
    whyItMatters: "It keeps domain logic cleaner and makes persistence concerns easier to change, test, and review in one place.",
    relatedTerms: ["unit of work", "aggregate", "clean architecture", "data mapper"],
  },
  {
    id: "unit-of-work",
    term: "Unit of Work",
    category: "design-patterns",
    tags: ["persistence", "transactions", "consistency"],
    oneLiner: "Tracks a set of changes and commits them together as one logical transaction.",
    body: "Unit of Work coordinates inserts, updates, and deletes that belong to the same business operation. Instead of writing immediately on every change, it keeps a change set and flushes it in one controlled commit. That makes it easier to preserve consistency when several objects must succeed or fail together.",
    example: "Creating an order updates the order, reserves inventory, and writes a payment record, then commits all of them at once.",
    whyItMatters: "It reduces partial writes and gives business workflows a clearer boundary for atomic persistence.",
    relatedTerms: ["repository pattern", "transaction", "aggregate", "outbox pattern"],
  },
  {
    id: "cqrs",
    term: "CQRS (Command Query Responsibility Segregation)",
    category: "design-patterns",
    tags: ["architecture", "reads-and-writes", "scaling"],
    oneLiner: "Separates write operations from read operations so each side can be modeled and optimized differently.",
    body: "CQRS says commands that change state and queries that read state have different needs and should not always share the same model. The write side focuses on invariants and correctness, while the read side focuses on retrieval and projection. In simple systems this split is overkill, but in complex workflows it can cleanly separate concerns.",
    example: "An order service writes through an aggregate model but serves dashboards from precomputed read tables tuned for filtering and search.",
    whyItMatters: "It helps systems scale and stay understandable when write rules are complex but read demands are broad and high volume.",
    relatedTerms: ["command", "event-driven architecture", "event sourcing", "read model"],
  },
  {
    id: "saga-pattern",
    term: "Saga Pattern",
    category: "design-patterns",
    tags: ["distributed-systems", "transactions", "workflow"],
    oneLiner: "Breaks a distributed transaction into a sequence of local steps with compensating actions when something fails.",
    body: "Saga is used when one business action spans multiple services that cannot share a single database transaction. Each service performs its local update and emits the next step, and failures trigger compensating actions to unwind prior work. Sagas can be orchestrated by a central coordinator or choreographed through events.",
    example: "A travel booking flow reserves a flight, hotel, and car separately, then cancels earlier reservations if the last step fails.",
    whyItMatters: "It gives distributed systems a practical way to preserve business correctness when classic all-or-nothing transactions are unavailable.",
    relatedTerms: ["outbox pattern", "event-driven architecture", "state", "compensation"],
  },
  {
    id: "strangler-fig-pattern",
    term: "Strangler Fig Pattern",
    category: "design-patterns",
    tags: ["migration", "legacy-systems", "incremental-change"],
    oneLiner: "Replaces a legacy system piece by piece by routing traffic to new parts as they become ready.",
    body: "Instead of rewriting everything in one large cutover, the Strangler Fig pattern grows a new system around the old one. Requests gradually move to the new implementation for selected features or endpoints. Over time the legacy system shrinks until it can be removed.",
    example: "A monolith keeps serving billing while new account and search routes are peeled off into separate services behind the same entry point.",
    whyItMatters: "It lowers migration risk by turning one dangerous rewrite into a series of controlled, testable replacements.",
    relatedTerms: ["facade", "anti-corruption layer", "hexagonal architecture", "migration seam"],
  },
  {
    id: "sidecar-pattern",
    term: "Sidecar Pattern",
    category: "design-patterns",
    tags: ["distributed-systems", "operations", "deployment"],
    oneLiner: "Runs a helper process alongside the main service so shared operational capabilities stay out of the app code.",
    body: "A sidecar lives next to an application instance and handles concerns like logging, service discovery, certificate rotation, or traffic policy. The main app talks locally to the sidecar instead of embedding those responsibilities itself. This keeps operational features reusable across services.",
    example: "A service sends telemetry to a local sidecar that batches and forwards metrics and traces to the platform.",
    whyItMatters: "It removes repeated infrastructure glue from application code and makes platform capabilities easier to roll out consistently.",
    relatedTerms: ["ambassador pattern", "proxy", "service mesh", "decorator"],
  },
  {
    id: "ambassador-pattern",
    term: "Ambassador Pattern",
    category: "design-patterns",
    tags: ["distributed-systems", "networking", "outbound-calls"],
    oneLiner: "Uses a local helper proxy to manage how a service talks to external systems.",
    body: "The Ambassador pattern focuses on outbound communication rather than general sidecar duties. A local proxy handles connection pooling, retries, TLS, discovery, or protocol translation before traffic leaves the service. The application uses the helper as its nearby representative to the outside world.",
    example: "An app connects to `localhost` while the ambassador handles secure communication and failover to a remote database cluster.",
    whyItMatters: "It simplifies external integrations and centralizes network behavior that would otherwise be duplicated in every client.",
    relatedTerms: ["sidecar pattern", "adapter", "proxy", "retry with exponential backoff"],
  },
  {
    id: "bulkhead-pattern",
    term: "Bulkhead Pattern",
    category: "design-patterns",
    tags: ["resilience", "isolation", "fault-tolerance"],
    oneLiner: "Isolates resources so one overloaded part of the system does not sink everything else with it.",
    body: "Bulkhead borrows the ship design idea of sealed compartments. In software, you isolate thread pools, connection pools, queues, or service instances so one dependency failure or traffic spike stays contained. Without that isolation, a single bad path can exhaust shared resources for all paths.",
    example: "Background report generation uses its own worker pool so a spike there cannot starve checkout requests.",
    whyItMatters: "It improves resilience by limiting blast radius when one workload or dependency starts failing badly.",
    relatedTerms: ["circuit breaker", "retry with exponential backoff", "rate limiting", "resource isolation"],
  },
  {
    id: "retry-with-exponential-backoff",
    term: "Retry with Exponential Backoff",
    category: "design-patterns",
    tags: ["resilience", "networking", "fault-tolerance"],
    oneLiner: "Retries transient failures, but waits longer after each miss so the system does not hammer a struggling dependency.",
    body: "Simple retries can turn a partial outage into a full meltdown by multiplying load exactly when a dependency is weakest. Exponential backoff spaces attempts out by increasing the delay after each failure, often with jitter to avoid synchronized retry storms. The pattern only makes sense for errors that are likely to recover and for operations that are safe to repeat.",
    example: "A worker retries a failed API call after 200 ms, then 400 ms, then 800 ms, adding random jitter on each wait.",
    whyItMatters: "It improves recovery odds during transient failures while reducing the chance that your own retry logic makes the outage worse.",
    relatedTerms: ["bulkhead pattern", "circuit breaker", "idempotency", "ambassador pattern"],
  },
  {
    id: "outbox-pattern",
    term: "Outbox Pattern",
    category: "design-patterns",
    tags: ["distributed-systems", "messaging", "consistency"],
    oneLiner: "Writes domain data and pending messages in the same local transaction so events are not lost or published twice by accident.",
    body: "The Outbox pattern solves the gap between changing a database and publishing an event. Instead of doing both independently, the service writes the domain update and an outbox record together. A separate relay then publishes from the outbox reliably and marks progress.",
    example: "When an order is placed, the service stores the order row and an `OrderPlaced` outbox entry in the same database commit.",
    whyItMatters: "It is one of the most practical ways to keep data changes and event publication consistent in distributed systems.",
    relatedTerms: ["event-driven architecture", "saga pattern", "unit of work", "idempotent consumer"],
  },
  {
    id: "event-driven-architecture",
    term: "Event-Driven Architecture",
    category: "design-patterns",
    tags: ["architecture", "messaging", "asynchronous-systems"],
    oneLiner: "Uses events to signal that something happened so other parts of the system can react without tight coupling.",
    body: "In event-driven architecture, producers emit facts about completed actions and consumers subscribe to react on their own timelines. This creates looser coupling than direct request-response chains, but it also introduces eventual consistency and more complex observability. The main discipline is defining events as stable business facts, not vague internal noise.",
    example: "After payment succeeds, a service emits `PaymentCaptured`, and inventory, shipping, analytics, and email systems each react independently.",
    whyItMatters: "It enables scalable, decoupled systems, but only when engineers understand the tradeoff between flexibility and coordination complexity.",
    relatedTerms: ["outbox pattern", "cqrs", "saga pattern", "pub/sub"],
  },
  {
    id: "hexagonal-architecture",
    term: "Hexagonal Architecture",
    category: "design-patterns",
    tags: ["architecture", "ports-and-adapters", "boundaries"],
    oneLiner: "Keeps core business logic in the center and pushes external concerns behind explicit ports and adapters.",
    body: "Hexagonal architecture defines the application core in terms of incoming and outgoing ports. Databases, web frameworks, message brokers, and other integrations plug in through adapters that satisfy those ports. The shape forces the domain to depend inward instead of letting infrastructure details leak everywhere.",
    example: "A use case talks to a `PaymentGateway` port, and separate Stripe and test adapters implement that contract outside the core.",
    whyItMatters: "It makes business logic easier to test and change because the core stays insulated from infrastructure churn.",
    relatedTerms: ["adapter", "clean architecture", "repository pattern", "ports and adapters"],
  },
  {
    id: "clean-architecture",
    term: "Clean Architecture",
    category: "design-patterns",
    tags: ["architecture", "boundaries", "dependency-rule"],
    oneLiner: "Organizes a system so high-level business rules stay independent from frameworks, databases, and delivery mechanisms.",
    body: "Clean architecture arranges code in layers with a strict dependency direction toward the center. Outer layers deal with UI, persistence, and infrastructure, while inner layers hold use cases and enterprise rules. The key rule is that policy should not know about delivery or storage details.",
    example: "An order use case can run from an HTTP controller today and a CLI or queue consumer tomorrow without rewriting the business rule itself.",
    whyItMatters: "It protects core behavior from framework churn and helps large codebases stay changeable over time.",
    relatedTerms: ["hexagonal architecture", "repository pattern", "use case", "dependency rule"],
  },
  {
    id: "entities-and-value-objects",
    term: "Entities and Value Objects",
    category: "design-patterns",
    tags: ["domain-driven-design", "modeling", "domain-model"],
    oneLiner: "Entities are defined by identity over time, while value objects are defined entirely by the data they hold.",
    body: "An entity remains the same conceptual thing even as some attributes change, such as a customer or invoice. A value object has no separate identity and is interchangeable with another object that has the same value, such as money, coordinates, or a date range. Good domain models use both intentionally instead of treating every structure like a mutable record.",
    example: "A `Customer` is an entity because it persists through changes, while `Money(10, 'USD')` is a value object because only its value matters.",
    whyItMatters: "This distinction shapes how you compare objects, model invariants, and decide what should be mutable in the domain.",
    relatedTerms: ["aggregate", "bounded context", "immutability", "domain model"],
  },
  {
    id: "aggregate",
    term: "Aggregate",
    category: "design-patterns",
    tags: ["domain-driven-design", "consistency", "transactions"],
    oneLiner: "A cluster of domain objects that is treated as one consistency boundary with a single root in charge.",
    body: "An aggregate groups related objects whose rules must stay consistent together. External code talks to the aggregate root, which protects invariants and controls changes to the rest of the cluster. The boundary should be small enough for transactional safety but rich enough to enforce business rules.",
    example: "An `Order` aggregate root controls its line items so discount and total rules cannot be bypassed by editing child objects directly.",
    whyItMatters: "It gives complex domains a concrete unit for enforcing invariants, persistence, and transactional boundaries.",
    relatedTerms: ["repository pattern", "unit of work", "entities and value objects", "domain invariants"],
  },
  {
    id: "bounded-context",
    term: "Bounded Context",
    category: "design-patterns",
    tags: ["domain-driven-design", "language", "service-boundaries"],
    oneLiner: "A bounded context defines where a model and its language are valid, so the same term does not mean everything everywhere.",
    body: "Large businesses reuse words like account, customer, order, and product in different ways across teams and workflows. A bounded context draws a line around one model so its meaning stays coherent inside that boundary. Translation happens at the edges instead of pretending one global model can serve every need.",
    example: "In billing, an account might mean who gets invoiced, while in identity it means login credentials and roles.",
    whyItMatters: "It prevents semantic confusion from leaking across teams and helps break large systems into clearer, safer boundaries.",
    relatedTerms: ["aggregate", "entities and value objects", "hexagonal architecture", "ubiquitous language"],
  },
];
