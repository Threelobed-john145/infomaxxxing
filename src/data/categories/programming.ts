import { Concept } from '@/lib/types';

export const programmingConcepts: Concept[] = [
  {
    id: "pure-functions",
    term: "Pure Functions",
    category: "programming",
    tags: ["functional-programming", "functions", "predictability"],
    oneLiner: "A pure function always returns the same output for the same input and does not change anything outside itself.",
    body: "Pure functions depend only on their arguments, not on hidden state, clocks, network calls, or global variables. That makes them easier to reason about because you can treat them like math: plug in values and get a result. They also avoid side effects, so calling them does not silently mutate other parts of the program.",
    example: "A function that turns a Celsius number into Fahrenheit is pure. A function that reads from a global config object or updates a database is not.",
    whyItMatters: "Pure functions are easier to test, cache, reuse, and parallelize. They shrink the surface area where bugs can hide because less behavior depends on invisible context.",
    relatedTerms: ["higher-order functions", "function composition", "immutability", "referential transparency"]
  },
  {
    id: "higher-order-functions",
    term: "Higher-Order Functions",
    category: "programming",
    tags: ["functional-programming", "functions", "abstraction"],
    oneLiner: "A higher-order function takes functions as input, returns a function, or both.",
    body: "This lets behavior become data that can be passed around, combined, and customized. Functions like map, filter, and reduce are classic examples because they accept another function that defines the work to do. Higher-order functions let you abstract over patterns without hardcoding every variation.",
    example: "Array.prototype.filter is higher-order because it takes a predicate function and keeps only the items for which that predicate returns true.",
    whyItMatters: "They are a core tool for writing reusable, expressive code. Many APIs, from event handlers to middleware to collection helpers, are built around this idea.",
    relatedTerms: ["pure functions", "function composition", "currying", "closures"]
  },
  {
    id: "function-composition",
    term: "Function Composition",
    category: "programming",
    tags: ["functional-programming", "pipelines", "abstraction"],
    oneLiner: "Function composition builds larger behavior by chaining small functions together.",
    body: "Instead of writing one giant function that does everything, you connect focused functions where the output of one becomes the input of the next. This works best when the pieces have simple, predictable interfaces. Composition turns programs into pipelines of transformations rather than deeply nested control flow.",
    example: "You might parse input, validate it, normalize it, and format it as four small functions composed into one pipeline.",
    whyItMatters: "Composition encourages small units with clear responsibilities. That usually leads to code that is easier to test, swap, and rearrange when requirements change.",
    relatedTerms: ["pure functions", "higher-order functions", "currying", "pipes"]
  },
  {
    id: "currying",
    term: "Currying",
    category: "programming",
    tags: ["functional-programming", "functions", "api-design"],
    oneLiner: "Currying rewrites a function of many arguments into a chain of functions that each take one argument.",
    body: "A curried function lets you supply arguments one at a time, producing a new function after each step. This makes partial application natural because you can pre-fill some inputs and reuse the specialized result later. It is less about performance and more about shaping APIs around composition.",
    example: "Instead of add(2, 3), a curried version is add(2)(3). Calling add(2) creates a new function that adds two to whatever comes next.",
    whyItMatters: "Currying helps build configurable, reusable helpers without creating many bespoke wrappers. It is common in functional libraries because it pairs well with composition and pipelines.",
    relatedTerms: ["higher-order functions", "function composition", "partial application", "closures"]
  },
  {
    id: "monads",
    term: "Monads",
    category: "programming",
    tags: ["functional-programming", "abstraction", "effects"],
    oneLiner: "A monad is a pattern for sequencing computations that carry extra context, like failure, optionality, or async work.",
    body: "Instead of passing around raw values, monads wrap values in a container that also encodes rules for what happens next. The key idea is that you can chain operations without manually unpacking and rewrapping at every step. Optional values, results, and promises are all practical examples of this pattern.",
    example: "A Result type can stop a pipeline on the first error without every function having to manually check for failure after each step.",
    whyItMatters: "Monads give a disciplined way to compose code that is not just plain value-in, value-out. Even if the formal theory feels abstract, the practical payoff is cleaner error handling and effect management.",
    relatedTerms: ["higher-order functions", "error handling patterns", "promises", "algebraic data types"]
  },
  {
    id: "pattern-matching",
    term: "Pattern Matching",
    category: "programming",
    tags: ["control-flow", "functional-programming", "types"],
    oneLiner: "Pattern matching selects behavior by checking whether data fits a known shape.",
    body: "Unlike a long series of if statements, pattern matching lets code branch directly on the structure of a value. It is especially powerful when paired with tagged unions or algebraic data types because the compiler can verify whether every case is handled. This often makes branching logic more explicit and safer.",
    example: "A response value with variants like Loading, Success, and Error can be matched so each state has its own clear branch.",
    whyItMatters: "It reduces fragile conditional logic and makes impossible states harder to ignore. In typed languages, exhaustive matching can catch missing cases before runtime.",
    relatedTerms: ["algebraic data types", "sum types", "exhaustiveness checking", "control flow"]
  },
  {
    id: "algebraic-data-types",
    term: "Algebraic Data Types",
    category: "programming",
    tags: ["types", "data-modeling", "functional-programming"],
    oneLiner: "Algebraic data types model data by combining simpler types into products and sums.",
    body: "A product type combines fields together, like a User with an id and email that both exist at once. A sum type represents alternatives, like a PaymentMethod that is either Card or BankTransfer. Together they let you model real states directly in the type system instead of encoding meaning with loose flags and nullable fields.",
    example: "A shape can be a Circle with a radius or a Rectangle with width and height. That is a sum type whose variants each contain their own product types.",
    whyItMatters: "ADTs make data models clearer and reduce invalid states. They pair naturally with pattern matching, which makes business logic easier to read and harder to get wrong.",
    relatedTerms: ["pattern matching", "sum types", "product types", "type modeling"]
  },
  {
    id: "generics-and-parametric-polymorphism",
    term: "Generics and Parametric Polymorphism",
    category: "programming",
    tags: ["types", "reusability", "abstraction"],
    oneLiner: "Generics let code work over many types while preserving type information.",
    body: "Instead of writing one list type for numbers and another for strings, you can write one generic list that works for any element type. Parametric polymorphism means the implementation does not care which concrete type fills the slot, only that the structure of the API remains valid. This gives reuse without falling back to untyped values.",
    example: "A function like identity<T>(value: T): T returns whatever type it receives while keeping that exact type at compile time.",
    whyItMatters: "Generics let libraries stay flexible without giving up safety. They are a major reason modern typed languages can be both reusable and precise.",
    relatedTerms: ["polymorphism", "monomorphization", "type inference", "structural typing"]
  },
  {
    id: "covariance-vs-contravariance",
    term: "Covariance vs Contravariance",
    category: "programming",
    tags: ["types", "subtyping", "function-types"],
    oneLiner: "Variance describes how subtype relationships behave when one type is nested inside another.",
    body: "Covariance means a container of a more specific type can be used where a less specific one is expected. Contravariance usually appears in function parameters, where accepting a broader input type is safer than requiring a narrower one. These rules exist to prevent type systems from approving substitutions that would break at runtime.",
    example: "If Cat is a subtype of Animal, a producer of Cats may be usable as a producer of Animals, but a consumer that only accepts Cats cannot safely stand in for one that must handle any Animal.",
    whyItMatters: "Variance explains many surprising type errors in languages with inheritance or generic APIs. Understanding it helps you design interfaces that are flexible without becoming unsound.",
    relatedTerms: ["polymorphism", "generics", "subtyping", "function types"]
  },
  {
    id: "ownership-and-borrowing",
    term: "Ownership and Borrowing",
    category: "programming",
    tags: ["rust", "memory", "safety"],
    oneLiner: "Ownership and borrowing control who is allowed to use data and for how long.",
    body: "In Rust-style systems, each value has an owner responsible for its lifetime, and references can temporarily borrow access without taking ownership. The compiler checks that borrowed references do not outlive the data they point to and that unsafe aliasing rules are not violated. This moves many memory bugs from runtime into compile-time errors.",
    example: "A function can borrow a string to read it without copying it, while the compiler prevents another part of the program from mutating that same string in an unsafe way at the same time.",
    whyItMatters: "This model enables memory safety without a tracing garbage collector. It is the foundation for building fast systems code while avoiding common C and C++ bugs.",
    relatedTerms: ["memory safety without garbage collection", "lifetimes", "references", "move semantics"]
  },
  {
    id: "memory-safety-without-gc",
    term: "Memory Safety Without Garbage Collection",
    category: "programming",
    tags: ["memory", "systems", "safety"],
    oneLiner: "Some languages prevent memory bugs without relying on a runtime garbage collector.",
    body: "Traditional manual memory management is fast but dangerous because dangling pointers, double frees, and use-after-free bugs are easy to introduce. Languages like Rust take a different path by enforcing lifetime and aliasing rules at compile time. The runtime stays lean, but the compiler does much more work to guarantee safety.",
    example: "Instead of periodically scanning the heap to discover dead objects, the compiler proves when values can be dropped safely as the program is built.",
    whyItMatters: "This approach is attractive for performance-sensitive software that still needs strong safety guarantees. It changes the tradeoff from runtime pauses to stricter compile-time rules.",
    relatedTerms: ["ownership and borrowing", "stack vs heap allocation", "reference counting", "use-after-free"]
  },
  {
    id: "reference-counting-vs-tracing-gc",
    term: "Reference Counting vs Tracing GC",
    category: "programming",
    tags: ["memory", "runtime", "garbage-collection"],
    oneLiner: "Reference counting and tracing garbage collection are two different ways to reclaim unused memory.",
    body: "Reference counting tracks how many live references point to a value and frees it when the count reaches zero. Tracing collectors start from known roots, find everything still reachable, and reclaim what is not. Reference counting gives more immediate cleanup but struggles with cycles, while tracing collectors handle cycles but may introduce pause times.",
    example: "Two objects that only point to each other can stay alive forever under plain reference counting, even if nothing else in the program can reach them.",
    whyItMatters: "Different memory strategies create different performance and ergonomics tradeoffs. Knowing the model helps explain behavior like latency spikes, destructor timing, and memory leaks caused by cycles.",
    relatedTerms: ["memory safety without garbage collection", "heap", "runtime", "ownership and borrowing"]
  },
  {
    id: "stack-vs-heap-allocation",
    term: "Stack vs Heap Allocation",
    category: "programming",
    tags: ["memory", "runtime", "performance"],
    oneLiner: "The stack is fast, structured storage for short-lived data, while the heap is flexible storage for data with dynamic lifetimes.",
    body: "Stack allocation is typically managed automatically as functions enter and leave scope, so allocating and cleaning up is cheap and predictable. Heap allocation supports values whose size or lifetime is not known ahead of time, but it needs more bookkeeping and often involves indirection. Many languages hide the distinction, but the tradeoff still shapes performance.",
    example: "A small local integer usually lives on the stack, while a dynamically sized object graph or string buffer often ends up on the heap.",
    whyItMatters: "This distinction helps explain allocation costs, pointer chasing, cache behavior, and why some values are cheap to create while others are not. It also underpins many conversations about lifetime, ownership, and runtime overhead.",
    relatedTerms: ["memory safety without garbage collection", "reference counting vs tracing gc", "tail call optimization", "allocation"]
  },
  {
    id: "tail-call-optimization",
    term: "Tail Call Optimization",
    category: "programming",
    tags: ["functions", "compilers", "performance"],
    oneLiner: "Tail call optimization lets certain recursive calls reuse the current stack frame instead of creating a new one.",
    body: "A tail call happens when the final action of a function is calling another function and immediately returning its result. In that case, a compiler or runtime can often replace recursion with a jump, avoiding stack growth. This makes some recursive styles as space-efficient as loops.",
    example: "A tail-recursive factorial helper that carries an accumulator can run in constant stack space if the language performs the optimization.",
    whyItMatters: "Without TCO, elegant recursive code can still crash on deep input. With it, recursion becomes a more practical tool for iterative processes and functional patterns.",
    relatedTerms: ["recursion", "stack vs heap allocation", "compiler vs interpreter vs jit", "functions"]
  },
  {
    id: "lazy-vs-eager-evaluation",
    term: "Lazy Evaluation vs Eager Evaluation",
    category: "programming",
    tags: ["evaluation", "performance", "language-design"],
    oneLiner: "Eager evaluation computes values right away, while lazy evaluation waits until the value is actually needed.",
    body: "Eager evaluation is straightforward because work happens where expressions appear. Lazy evaluation can avoid unnecessary computation and support infinite data structures, but it also makes execution order less obvious. The choice affects performance, memory use, and how easy it is to predict side effects.",
    example: "An eager language will build an entire filtered list now, while a lazy pipeline may only produce the first few items when a caller asks for them.",
    whyItMatters: "This tradeoff shapes how APIs behave and where costs show up. It helps explain why some code feels immediate and predictable while other code is more composable but harder to profile.",
    relatedTerms: ["iterators and generators", "tail call optimization", "pure functions", "evaluation strategy"]
  },
  {
    id: "iterators-and-generators",
    term: "Iterators and Generators",
    category: "programming",
    tags: ["iteration", "lazy-evaluation", "language-features"],
    oneLiner: "Iterators define a step-by-step way to produce values, and generators are a convenient way to build them.",
    body: "An iterator exposes a protocol for asking for the next item in a sequence. A generator lets you write that sequence as ordinary-looking code that can pause and resume between yields. Together they make it easy to model streams, pipelines, and large sequences without materializing everything at once.",
    example: "A generator can yield lines from a file one at a time instead of loading the whole file into memory before processing starts.",
    whyItMatters: "They support efficient, composable data processing and are a common foundation for lazy APIs. Understanding them also helps when working with async iteration and stream-based designs.",
    relatedTerms: ["lazy evaluation vs eager evaluation", "coroutines and green threads", "function composition", "streams"]
  },
  {
    id: "coroutines-and-green-threads",
    term: "Coroutines and Green Threads",
    category: "programming",
    tags: ["concurrency", "runtime", "scheduling"],
    oneLiner: "Coroutines and green threads let many tasks make progress without mapping each task directly to an operating system thread.",
    body: "A coroutine is a routine that can suspend and later resume from where it left off, usually at explicit yield or await points. Green threads are lightweight user-space threads scheduled by a runtime instead of the OS. They are related because both reduce the overhead of handling many waiting tasks, but they differ in how scheduling and preemption work.",
    example: "A server can keep thousands of network requests in flight by parking tasks when they wait on I/O and resuming them when data is ready.",
    whyItMatters: "These models make high-concurrency programs practical without spawning thousands of expensive OS threads. They also explain the behavior of async runtimes in languages like Go, Kotlin, and Rust.",
    relatedTerms: ["iterators and generators", "actor model", "message passing vs shared memory", "async await"]
  },
  {
    id: "actor-model",
    term: "Actor Model",
    category: "programming",
    tags: ["concurrency", "distributed-systems", "erlang"],
    oneLiner: "The actor model structures concurrent programs as isolated actors that communicate only by sending messages.",
    body: "Each actor owns its own state and processes one message at a time, which avoids many shared-state race conditions. Actors can create other actors, send messages, and decide how to respond based on incoming messages. Erlang and Elixir made this model famous for building fault-tolerant systems.",
    example: "A chat service might model each room as an actor that receives join, leave, and post-message commands and updates its internal state in order.",
    whyItMatters: "The actor model gives a clean mental model for concurrency and distribution. It trades shared memory complexity for explicit communication and supervision strategies.",
    relatedTerms: ["message passing vs shared memory", "coroutines and green threads", "supervision trees", "distributed systems"]
  },
  {
    id: "message-passing-vs-shared-memory",
    term: "Message Passing vs Shared Memory",
    category: "programming",
    tags: ["concurrency", "architecture", "tradeoffs"],
    oneLiner: "Concurrent systems can coordinate by sending messages or by reading and writing the same memory.",
    body: "Shared memory can be fast and direct, but it needs locks, atomics, or other synchronization to avoid races. Message passing keeps state more isolated by moving information between components through queues or channels. Neither approach is universally better, because the right choice depends on throughput, isolation, and coordination needs.",
    example: "Threads updating one shared counter need synchronization, while worker processes sending increments over a channel avoid shared writes but pay messaging overhead.",
    whyItMatters: "This is one of the main design choices in concurrent systems. It affects debuggability, performance, fault isolation, and how much accidental coupling builds up in the code.",
    relatedTerms: ["actor model", "ownership and borrowing", "coroutines and green threads", "race conditions"]
  },
  {
    id: "duck-typing-and-structural-typing",
    term: "Duck Typing and Structural Typing",
    category: "programming",
    tags: ["types", "language-design", "interfaces"],
    oneLiner: "Both ideas care about what a value can do, but duck typing checks at runtime while structural typing checks by shape ahead of time.",
    body: "Duck typing says that if an object behaves like the expected thing, it is acceptable, even if it does not explicitly declare a relationship. Structural typing formalizes a similar idea in the type system by checking whether a value has the required fields or methods. One is flexible at runtime, while the other gives earlier feedback during development.",
    example: "A function that needs a toJSON method may accept any object with that method in a duck-typed language, while TypeScript can enforce the same shape structurally before the code runs.",
    whyItMatters: "This distinction explains why some languages feel very permissive and others catch shape mismatches earlier. It also affects how libraries define contracts and how easy it is to plug new types into existing APIs.",
    relatedTerms: ["generics and parametric polymorphism", "interface vs abstract class", "polymorphism", "subtyping"]
  },
  {
    id: "metaprogramming-reflection-and-macros",
    term: "Metaprogramming, Reflection, and Macros",
    category: "programming",
    tags: ["language-features", "tooling", "abstraction"],
    oneLiner: "These techniques let programs inspect, generate, or transform code instead of only executing it.",
    body: "Metaprogramming is the broad idea of writing code that works with code. Reflection inspects program structure at runtime, such as types, fields, or methods, while macros transform code before normal execution, often at compile time. They can remove boilerplate and unlock powerful abstractions, but they can also make behavior harder to trace.",
    example: "A framework might use reflection to find annotated handlers, while a macro might generate repetitive serialization code from a type definition.",
    whyItMatters: "These tools can dramatically improve developer productivity when used carefully. They also change how readable, debuggable, and explicit a codebase feels, so they deserve deliberate use.",
    relatedTerms: ["abstract syntax tree", "transpilation and code generation", "compiler vs interpreter vs jit", "dependency injection"]
  },
  {
    id: "abstract-syntax-tree",
    term: "Abstract Syntax Tree",
    category: "programming",
    tags: ["compilers", "parsing", "tooling"],
    oneLiner: "An abstract syntax tree is a structured representation of source code after parsing.",
    body: "Instead of treating code as raw text, an AST captures meaning as nodes like function declarations, variable references, and expressions. Tools can then analyze or rewrite code by walking that tree rather than doing fragile string manipulation. Most compilers, linters, formatters, and codemods depend on ASTs.",
    example: "A formatter can inspect an AST node for a call expression and reprint it consistently regardless of how the original source was spaced.",
    whyItMatters: "ASTs are the bridge between human-written source code and machine-driven tooling. If you understand them, a wide range of build tools and code transforms become much less mysterious.",
    relatedTerms: ["metaprogramming, reflection, and macros", "compiler vs interpreter vs jit", "transpilation and code generation", "parsing"]
  },
  {
    id: "compiler-vs-interpreter-vs-jit",
    term: "Compiler vs Interpreter vs JIT",
    category: "programming",
    tags: ["runtime", "language-design", "performance"],
    oneLiner: "Compilers translate code ahead of time, interpreters execute it directly, and JITs compile hot code during execution.",
    body: "A compiler usually turns source code into another form before the program runs, which can enable strong optimization and early error checking. An interpreter reads and executes instructions on the fly, often making development more flexible. A just-in-time compiler mixes both approaches by observing runtime behavior and optimizing the parts that matter most.",
    example: "Rust is mostly ahead-of-time compiled, a classic shell language is interpreted, and JavaScript engines use JIT compilation for frequently executed code paths.",
    whyItMatters: "These execution models explain startup time, runtime speed, tooling behavior, and deployment shape. They also affect what kinds of optimizations and guarantees a language ecosystem can provide.",
    relatedTerms: ["transpilation and code generation", "abstract syntax tree", "tail call optimization", "runtime"]
  },
  {
    id: "transpilation-and-code-generation",
    term: "Transpilation and Code Generation",
    category: "programming",
    tags: ["build-tools", "compilers", "automation"],
    oneLiner: "Transpilation rewrites code into another source language, while code generation produces code from a higher-level description.",
    body: "A transpiler usually converts one source language into another at roughly the same level of abstraction, such as TypeScript into JavaScript. Code generation is broader and may produce source files, clients, schemas, or boilerplate from definitions like APIs or models. Both techniques automate repetitive work and bridge incompatible environments.",
    example: "A tool can generate a typed API client from an OpenAPI spec, or transpile modern syntax into older JavaScript that more browsers understand.",
    whyItMatters: "These workflows shape modern builds, framework tooling, and SDK generation. They save time when done well, but they also add another layer that developers must understand and debug.",
    relatedTerms: ["abstract syntax tree", "compiler vs interpreter vs jit", "serialization and deserialization", "build pipeline"]
  },
  {
    id: "serialization-and-deserialization",
    term: "Serialization and Deserialization",
    category: "programming",
    tags: ["data-formats", "networking", "storage"],
    oneLiner: "Serialization turns in-memory data into a transferable format, and deserialization turns it back into usable program data.",
    body: "Programs often need to move data across a network, write it to disk, or send it between services. Formats like JSON, Protobuf, and MessagePack make that possible, each with different tradeoffs around size, speed, readability, and schema strictness. Deserialization is not just parsing bytes, because it also rebuilds structure and meaning in the target program.",
    example: "An API server may serialize a response as JSON for browsers, while internal services exchange Protobuf messages to reduce payload size and improve consistency.",
    whyItMatters: "Data boundaries are where many bugs and compatibility issues show up. Choosing the right format affects performance, interoperability, and how safely systems can evolve over time.",
    relatedTerms: ["transpilation and code generation", "dependency management", "semantic versioning", "schema"]
  },
  {
    id: "semantic-versioning",
    term: "Semantic Versioning",
    category: "programming",
    tags: ["release-management", "dependencies", "versioning"],
    oneLiner: "Semantic versioning uses version numbers to signal whether a release is compatible, additive, or breaking.",
    body: "The common format is MAJOR.MINOR.PATCH. A patch release fixes bugs without changing the public contract, a minor release adds backward-compatible functionality, and a major release introduces breaking changes. The system only works if maintainers apply those labels honestly and consistently.",
    example: "Moving from 2.4.1 to 2.4.2 should be low risk, while moving from 2.4.1 to 3.0.0 signals that downstream code may need changes.",
    whyItMatters: "Version numbers are how teams communicate upgrade risk at a glance. When semver is followed well, dependency updates become much easier to plan and automate.",
    relatedTerms: ["dependency management", "public api", "breaking changes", "release process"]
  },
  {
    id: "dependency-management",
    term: "Dependency Management",
    category: "programming",
    tags: ["packages", "tooling", "builds"],
    oneLiner: "Dependency management is the practice of declaring, resolving, locking, and updating the external code your project relies on.",
    body: "Modern projects rarely stand alone, so package managers track version constraints and download the matching libraries. Lockfiles pin exact resolved versions so installs stay reproducible across machines and time. The hard part is balancing freshness, security, and compatibility as upstream packages evolve.",
    example: "Two libraries may require overlapping but incompatible versions of the same package, forcing the resolver or maintainer to make a tradeoff.",
    whyItMatters: "A large share of build failures, supply chain risk, and upgrade pain comes from dependency issues rather than product code. Good dependency hygiene keeps the project stable and easier to maintain.",
    relatedTerms: ["semantic versioning", "serialization and deserialization", "technical debt", "supply chain"]
  },
  {
    id: "monomorphization",
    term: "Monomorphization",
    category: "programming",
    tags: ["generics", "compilers", "performance"],
    oneLiner: "Monomorphization turns generic code into concrete specialized versions for each type it is used with.",
    body: "Instead of one shared implementation handling all types indirectly, the compiler can generate a separate version for numbers, strings, or any other concrete instantiation. This often improves optimization because the compiler knows the exact types involved. The tradeoff is larger compiled output and more work during compilation.",
    example: "A generic max function might become one machine-level version for integers and another for floating-point values.",
    whyItMatters: "This explains why some generic code is surprisingly fast in ahead-of-time compiled languages. It also clarifies why generics can increase binary size even when source code looks compact.",
    relatedTerms: ["generics and parametric polymorphism", "compiler vs interpreter vs jit", "polymorphism", "optimization"]
  },
  {
    id: "polymorphism",
    term: "Polymorphism",
    category: "programming",
    tags: ["types", "abstraction", "oop"],
    oneLiner: "Polymorphism means one interface can support many concrete forms of behavior.",
    body: "There are several flavors of polymorphism. Ad-hoc polymorphism picks an implementation based on a specific type, parametric polymorphism writes one implementation that works over many types, and subtype polymorphism lets different types stand in behind a shared contract. The common theme is that callers can write to a general abstraction rather than hardcoding every concrete case.",
    example: "A print operation might work differently for strings, numbers, and custom objects, while the calling code still uses one general interface.",
    whyItMatters: "Polymorphism is a central tool for extensibility and reuse. It helps decouple callers from implementations, but only when the shared abstraction is actually coherent.",
    relatedTerms: ["generics and parametric polymorphism", "covariance vs contravariance", "interface vs abstract class", "duck typing and structural typing"]
  },
  {
    id: "interface-vs-abstract-class",
    term: "Interface vs Abstract Class",
    category: "programming",
    tags: ["oop", "abstraction", "api-design"],
    oneLiner: "An interface defines a contract, while an abstract class can define both a contract and shared implementation.",
    body: "Interfaces focus on what a type must provide, not how it does it. Abstract classes can bundle partial behavior, shared state, or default methods along with abstract members that subclasses must fill in. The right choice depends on whether you need a pure capability boundary or a reusable base with built-in behavior.",
    example: "A payment provider interface might require charge and refund methods, while an abstract base class could also include shared logging or retry logic used by concrete providers.",
    whyItMatters: "This decision shapes coupling and reuse. Interfaces keep relationships looser, while abstract classes can reduce duplication at the cost of a stronger inheritance tie.",
    relatedTerms: ["mixins and traits", "polymorphism", "duck typing and structural typing", "inheritance"]
  },
  {
    id: "mixins-and-traits",
    term: "Mixins and Traits",
    category: "programming",
    tags: ["code-reuse", "oop", "composition"],
    oneLiner: "Mixins and traits let types reuse behavior without relying only on a single inheritance chain.",
    body: "A mixin typically injects reusable behavior into a type, while a trait often defines reusable methods plus explicit conflict rules for composition. Both approaches aim to share behavior horizontally rather than forcing everything through one rigid base class hierarchy. They are useful when several unrelated types need the same capability.",
    example: "A Timestamped trait can add createdAt and updatedAt behavior to multiple models without making them all inherit from one heavyweight parent class.",
    whyItMatters: "They offer a middle ground between copy-pasting behavior and deep inheritance trees. Used well, they improve reuse without collapsing unrelated concepts into the same class family.",
    relatedTerms: ["interface vs abstract class", "polymorphism", "function composition", "code reuse"]
  },
  {
    id: "error-handling-patterns",
    term: "Error Handling Patterns",
    category: "programming",
    tags: ["reliability", "apis", "control-flow"],
    oneLiner: "Programs can represent failure with exceptions, explicit result values like Result or Either, or low-level error codes.",
    body: "Exceptions separate the happy path from error propagation, which can make code cleaner but also hide control flow. Result and Either types force callers to acknowledge success or failure explicitly, trading convenience for clarity. Error codes are simple and portable, but they often provide less structure and are easier to ignore by accident.",
    example: "A file read might throw an exception, return a Result<File, Error>, or return a null value plus a numeric code depending on the language and style.",
    whyItMatters: "Error handling shapes API design, correctness, and observability. Teams that choose a pattern deliberately usually build systems that fail more predictably and are easier to debug.",
    relatedTerms: ["monads", "pattern matching", "polymorphism", "technical debt"]
  },
  {
    id: "property-based-testing",
    term: "Property-Based Testing",
    category: "programming",
    tags: ["testing", "quality", "automation"],
    oneLiner: "Property-based testing checks general truths about code by generating many inputs instead of hand-writing a few examples.",
    body: "Traditional tests often say that input X should produce output Y. Property-based tests go broader by stating invariants like sorting never loses elements and always returns an ordered result. A test library then generates large numbers of cases, including edge cases, to try to break that property.",
    example: "For a parser and serializer, a property might say that serializing a valid value and then parsing it should give back the original value.",
    whyItMatters: "This approach finds corner cases that example-based tests often miss. It is especially valuable for functions with large input spaces, transformations, and invariants that should always hold.",
    relatedTerms: ["fuzzing", "pure functions", "serialization and deserialization", "testing"]
  },
  {
    id: "fuzzing",
    term: "Fuzzing",
    category: "programming",
    tags: ["testing", "security", "reliability"],
    oneLiner: "Fuzzing bombards a program with unexpected or random inputs to uncover crashes, hangs, and security flaws.",
    body: "A fuzzer generates many malformed, extreme, or semi-valid inputs and feeds them to a target automatically. Good fuzzers keep the interesting cases that trigger new paths or failures and use them to guide future exploration. This makes fuzzing especially strong at finding parser bugs, memory corruption, and edge cases humans did not think to test.",
    example: "A file parser can be fuzzed with mutated binary files until one input causes a crash, revealing a bug that ordinary tests never covered.",
    whyItMatters: "Fuzzing is one of the most effective ways to shake out dangerous input-handling bugs. It complements normal tests by exploring weird cases at a scale that manual writing cannot match.",
    relatedTerms: ["property-based testing", "serialization and deserialization", "memory safety without garbage collection", "security testing"]
  },
  {
    id: "technical-debt",
    term: "Technical Debt",
    category: "programming",
    tags: ["maintenance", "tradeoffs", "engineering"],
    oneLiner: "Technical debt is the future cost created by taking a quicker or weaker implementation path today.",
    body: "Debt is not just messy code. It includes missing tests, brittle architecture, outdated dependencies, confusing interfaces, and shortcuts that make future change slower or riskier. Some debt is a conscious tradeoff, but unmanaged debt compounds because each new change has to fight the old compromises.",
    example: "Shipping a feature by duplicating logic in five places may save a day now, but every later rule change must touch all five copies and risks drifting out of sync.",
    whyItMatters: "Technical debt quietly taxes delivery speed and reliability until teams feel like every change is expensive. Naming it clearly helps teams decide which shortcuts are acceptable and which ones are already costing too much.",
    relatedTerms: ["dependency management", "error handling patterns", "dry", "refactoring"]
  }
];
