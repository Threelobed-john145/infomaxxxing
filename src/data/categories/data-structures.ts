import { Concept } from '@/lib/types';

export const dataStructuresConcepts: Concept[] = [
  {
    id: "heap",
    term: "Heap",
    category: "data-structures",
    tags: ["tree", "priority", "fundamentals"],
    oneLiner: "A heap keeps the most important item at the top without fully sorting everything.",
    body: "A heap is a specialized tree usually stored in an array. It follows a heap property: in a max-heap every parent is at least as large as its children, and in a min-heap every parent is at most as small. That rule makes the top element easy to read or remove, while the rest stays only partially ordered.",
    example: "A scheduler can keep the next job to run at the root instead of sorting the full job list after every insert.",
    whyItMatters: "Heaps are the standard way to build fast priority-based systems like schedulers, shortest-path algorithms, and event queues.",
    relatedTerms: ["min-heap", "max-heap", "priority queue", "binary tree"]
  },
  {
    id: "min-heap",
    term: "Min-Heap",
    category: "data-structures",
    tags: ["heap", "ordering", "algorithms"],
    oneLiner: "A min-heap always puts the smallest item where you can grab it first.",
    body: "In a min-heap, every node is less than or equal to its children. That guarantees the minimum value sits at the root, while inserts and removals reshuffle only a short path through the tree. It is commonly represented as an array, which keeps it compact and fast.",
    example: "Dijkstra's algorithm uses a min-heap to pull the next closest node with the lowest current distance.",
    whyItMatters: "Min-heaps are the default tool when you repeatedly need the smallest thing next without paying the cost of sorting everything.",
    relatedTerms: ["heap", "priority queue", "dijkstra's algorithm", "binary heap"]
  },
  {
    id: "max-heap",
    term: "Max-Heap",
    category: "data-structures",
    tags: ["heap", "ordering", "selection"],
    oneLiner: "A max-heap keeps the largest item ready at the top.",
    body: "In a max-heap, every parent is greater than or equal to its children. The biggest value is always at the root, so extracting the maximum is cheap. New items bubble upward only as far as needed to restore the heap property.",
    example: "A leaderboard can use a max-heap to keep the current highest score immediately available.",
    whyItMatters: "Max-heaps are useful when the highest-priority or largest item must be served first, such as ranking, scheduling, or top-k selection.",
    relatedTerms: ["heap", "priority queue", "heapsort", "binary heap"]
  },
  {
    id: "priority-queue",
    term: "Priority Queue",
    category: "data-structures",
    tags: ["queue", "priority", "scheduling"],
    oneLiner: "A priority queue serves the most important item first, not the oldest one.",
    body: "A priority queue stores items with an associated priority and removes the highest or lowest priority item before the rest. It is often implemented with a heap, though other structures can work too. Unlike a normal queue, arrival order matters only when priorities tie or when the implementation preserves stability.",
    example: "An operating system can run urgent tasks before background work even if the urgent tasks arrived later.",
    whyItMatters: "Priority queues show up in schedulers, graph algorithms, simulation engines, and any system where urgency matters more than arrival time.",
    relatedTerms: ["heap", "queue", "min-heap", "max-heap"]
  },
  {
    id: "adjacency-list",
    term: "Adjacency List",
    category: "data-structures",
    tags: ["graph", "representation", "memory"],
    oneLiner: "An adjacency list stores each graph node next to the nodes it connects to.",
    body: "This representation keeps, for every vertex, a list of its outgoing neighbors. It uses space roughly proportional to the number of vertices plus edges, so it stays efficient for sparse graphs. Iterating over a node's direct neighbors is especially fast because they are stored together.",
    example: "For a social graph, each user record can hold the list of friends or follows for that user.",
    whyItMatters: "Adjacency lists are the standard graph representation for most real systems because real graphs are usually sparse, not fully connected.",
    relatedTerms: ["graph", "adjacency matrix", "dag", "graph traversal"]
  },
  {
    id: "adjacency-matrix",
    term: "Adjacency Matrix",
    category: "data-structures",
    tags: ["graph", "representation", "dense-graphs"],
    oneLiner: "An adjacency matrix uses a grid to mark whether every pair of graph nodes is connected.",
    body: "The structure is a two-dimensional table where row i and column j record the edge from node i to node j. It uses more memory than an adjacency list because it stores a slot for every possible pair, even when most edges do not exist. The tradeoff is that checking whether a specific edge exists is immediate.",
    example: "In a fully connected network model, a matrix can quickly answer whether machine 4 connects directly to machine 9.",
    whyItMatters: "Adjacency matrices are useful for dense graphs and algorithms that constantly test edge existence between arbitrary pairs of nodes.",
    relatedTerms: ["graph", "adjacency list", "dense graph", "matrix"]
  },
  {
    id: "red-black-tree",
    term: "Red-Black Tree",
    category: "data-structures",
    tags: ["balanced-tree", "ordered-map", "search"],
    oneLiner: "A red-black tree stays roughly balanced by enforcing a few color rules on nodes.",
    body: "Each node is marked red or black, and those colors constrain how paths can grow. After inserts and deletes, the tree uses rotations and recoloring to keep the height from drifting too far. That gives predictable logarithmic performance for search, insertion, and deletion.",
    whyItMatters: "Many standard library ordered maps and sets use red-black trees because they balance good worst-case performance with relatively simple maintenance rules.",
    relatedTerms: ["avl tree", "binary search tree", "rotation", "balanced tree"]
  },
  {
    id: "avl-tree",
    term: "AVL Tree",
    category: "data-structures",
    tags: ["balanced-tree", "search", "rotation"],
    oneLiner: "An AVL tree keeps a search tree tightly balanced by limiting how uneven subtrees can get.",
    body: "For every node, the heights of the left and right subtrees may differ by at most one. When an insert or delete breaks that rule, the tree performs rotations to restore balance. This stricter balancing often makes lookups faster than in looser balanced trees, at the cost of more rebalancing work.",
    whyItMatters: "AVL trees are useful when fast reads matter and you want guaranteed logarithmic performance from an ordered structure.",
    relatedTerms: ["red-black tree", "binary search tree", "rotation", "balanced tree"]
  },
  {
    id: "b-tree",
    term: "B-Tree",
    category: "data-structures",
    tags: ["database", "index", "storage"],
    oneLiner: "A B-tree stores many keys per node so large ordered indexes stay shallow.",
    body: "Unlike a binary tree, a B-tree node can hold many keys and many children. That high branching factor keeps the tree height small, which matters when nodes live on disk and each level can cost an I/O. Insertions and deletions split or merge nodes to maintain balance.",
    example: "Database indexes use B-trees so a lookup can touch only a few disk pages even with millions of rows.",
    whyItMatters: "B-trees are the foundation of storage engines and file systems because they are built around the real cost of slow disk and page reads.",
    relatedTerms: ["b+ tree", "database index", "balanced tree", "page"]
  },
  {
    id: "b-plus-tree",
    term: "B+ Tree",
    category: "data-structures",
    tags: ["database", "index", "range-queries"],
    oneLiner: "A B+ tree stores all actual records in leaves and links those leaves for fast scans.",
    body: "Internal nodes in a B+ tree hold only keys for navigation, while leaf nodes hold the full records or pointers to them. The leaves are usually linked from left to right, which makes ordered range scans efficient. Like a B-tree, it stays balanced and keeps height low.",
    example: "A query for all users created between two dates can walk linked leaves instead of jumping around the tree.",
    whyItMatters: "B+ trees are common in databases because they combine fast point lookups with very efficient ordered iteration and range queries.",
    relatedTerms: ["b-tree", "database index", "range scan", "leaf node"]
  },
  {
    id: "skip-list",
    term: "Skip List",
    category: "data-structures",
    tags: ["ordered", "probabilistic", "search"],
    oneLiner: "A skip list speeds up a linked list by adding express lanes above the main list.",
    body: "Items appear in sorted order on a base linked list, and some items are promoted to higher levels with longer jumps. Searching starts high and drops down levels as needed, which gives expected logarithmic time. The structure uses randomness instead of complex balancing rules.",
    example: "Redis uses skip lists to support sorted sets where both lookup and range queries need to stay fast.",
    whyItMatters: "Skip lists offer a simpler alternative to balanced trees for ordered data, especially in systems that prefer straightforward concurrent or incremental updates.",
    relatedTerms: ["linked list", "balanced tree", "probabilistic data structure", "sorted set"]
  },
  {
    id: "segment-tree",
    term: "Segment Tree",
    category: "data-structures",
    tags: ["range-query", "tree", "aggregation"],
    oneLiner: "A segment tree stores summaries of array ranges so interval queries stay fast.",
    body: "Each node represents a segment of an array and stores a combined value such as a sum, minimum, or maximum for that segment. Queries break a requested interval into a small number of covered segments, and updates change only the path that contains the modified element. This supports fast range queries with updates on the same data.",
    example: "You can track the minimum temperature across any date range while still updating individual days efficiently.",
    whyItMatters: "Segment trees are a core tool when you need both frequent range queries and frequent point updates on ordered data.",
    relatedTerms: ["fenwick tree", "range query", "interval", "tree"]
  },
  {
    id: "fenwick-tree",
    term: "Fenwick Tree (Binary Indexed Tree)",
    category: "data-structures",
    tags: ["range-query", "prefix-sum", "array"],
    oneLiner: "A Fenwick tree keeps prefix totals in a compact structure so updates and sum queries stay quick.",
    body: "It stores partial sums in an array, where each position covers a power-of-two-sized range. That layout lets you move through the structure using bit tricks to update or query only a few entries. It is simpler and smaller than a segment tree when prefix-style aggregates are enough.",
    example: "A scoreboard can update one player's points and still answer total points up to rank 5000 efficiently.",
    whyItMatters: "Fenwick trees are a practical choice for cumulative totals because they are fast, compact, and easier to implement than heavier range-query structures.",
    relatedTerms: ["segment tree", "prefix sum", "range query", "bit manipulation"]
  },
  {
    id: "disjoint-set",
    term: "Disjoint Set (Union-Find)",
    category: "data-structures",
    tags: ["sets", "connectivity", "graphs"],
    oneLiner: "Union-find tracks which items belong to the same group as groups merge over time.",
    body: "Each item starts in its own set, and the structure supports two main operations: find which set an item belongs to, and union two sets together. With path compression and union by rank or size, those operations become extremely fast in practice. It is especially good at dynamic connectivity problems.",
    example: "As network cables are added between machines, union-find can tell whether two machines are now in the same connected component.",
    whyItMatters: "Union-find is the standard way to reason about merging groups, connected components, and cycle detection in graph algorithms.",
    relatedTerms: ["graph", "connected components", "kruskal's algorithm", "set"]
  },
  {
    id: "circular-buffer",
    term: "Circular Buffer (Ring Buffer)",
    category: "data-structures",
    tags: ["buffer", "streaming", "queue"],
    oneLiner: "A circular buffer reuses a fixed block of memory by wrapping reads and writes around in a loop.",
    body: "It treats an array as if the end connects back to the beginning. Head and tail positions move forward modulo the array size, so old space gets reused without shifting elements around. Some designs overwrite old data when full, while others reject new writes until space frees up.",
    example: "Audio software can keep the latest stream samples in a ring buffer without constantly allocating new memory.",
    whyItMatters: "Ring buffers are a common fit for streaming, logging, and producer-consumer pipelines where bounded memory and steady throughput matter.",
    relatedTerms: ["queue", "buffer", "deque", "producer-consumer"]
  },
  {
    id: "lru-cache",
    term: "LRU Cache",
    category: "data-structures",
    tags: ["cache", "eviction", "systems"],
    oneLiner: "An LRU cache keeps recently used items and evicts the least recently used one first.",
    body: "A typical implementation combines a hash map for direct lookup with a doubly linked list for usage order. Every access moves the item to the front, and when capacity is full the item at the back gets evicted. This matches workloads where recently touched data is likely to be touched again soon.",
    example: "A browser can keep recently opened pages or assets in memory and drop the stalest ones when space runs out.",
    whyItMatters: "LRU is one of the simplest and most widely used cache eviction policies, and its implementation teaches how structures can be combined for two performance goals at once.",
    relatedTerms: ["cache", "linked list", "hash table", "eviction policy"]
  },
  {
    id: "deque",
    term: "Deque (Double-Ended Queue)",
    category: "data-structures",
    tags: ["queue", "linear", "operations"],
    oneLiner: "A deque lets you add or remove items from both the front and the back.",
    body: "It generalizes both stacks and queues by supporting efficient operations at either end. Depending on the implementation, it may use a circular array or linked blocks to avoid expensive shifts. That flexibility makes it useful for sliding windows, task scheduling, and undo-like behavior.",
    example: "A sliding window maximum algorithm uses a deque to keep candidates in order while dropping stale values from the front.",
    whyItMatters: "A deque is the right fit when you need constant-time access at both ends instead of being forced into stack-only or queue-only behavior.",
    relatedTerms: ["queue", "stack", "circular buffer", "sliding window"]
  },
  {
    id: "suffix-array",
    term: "Suffix Array",
    category: "data-structures",
    tags: ["strings", "index", "search"],
    oneLiner: "A suffix array sorts all suffixes of a string so substring searches become efficient.",
    body: "Take every suffix of a string, sort them lexicographically, and store their starting positions. Once sorted, binary search can find whether a pattern appears and where matching regions begin. It is usually more compact than a suffix tree, though some operations are slower.",
    example: "For the string banana, the sorted suffixes help locate occurrences of ana without scanning every position.",
    whyItMatters: "Suffix arrays are a practical text indexing tool in search, compression, and bioinformatics when memory matters.",
    relatedTerms: ["suffix tree", "string search", "binary search", "index"]
  },
  {
    id: "suffix-tree",
    term: "Suffix Tree",
    category: "data-structures",
    tags: ["strings", "index", "tree"],
    oneLiner: "A suffix tree stores all suffixes of a string in one compressed tree for very fast pattern work.",
    body: "Each path from the root corresponds to a substring, and shared prefixes are stored once. That makes many string queries, like substring existence or repeated pattern discovery, very fast after the tree is built. The downside is complexity and memory overhead compared with simpler indexes.",
    example: "A DNA analysis tool can use a suffix tree to find repeated motifs across a long genome sequence.",
    whyItMatters: "Suffix trees are a classic structure for advanced string problems because they turn expensive repeated scans into direct index lookups.",
    relatedTerms: ["suffix array", "trie", "string search", "compressed tree"]
  },
  {
    id: "sparse-table",
    term: "Sparse Table",
    category: "data-structures",
    tags: ["range-query", "precomputation", "static-data"],
    oneLiner: "A sparse table answers range queries fast by precomputing results for power-of-two intervals.",
    body: "It works best on data that does not change after build time. The table stores answers for intervals of length 1, 2, 4, 8, and so on, which can be combined to answer queries quickly. For operations like minimum or maximum, queries can become constant time after preprocessing.",
    example: "If you need many minimum queries over a fixed log file, a sparse table can answer them without rebuilding anything.",
    whyItMatters: "Sparse tables are ideal when reads are heavy, updates do not happen, and you want faster queries than dynamic range structures can offer.",
    relatedTerms: ["segment tree", "range minimum query", "preprocessing", "static array"]
  },
  {
    id: "rope",
    term: "Rope",
    category: "data-structures",
    tags: ["strings", "editor", "tree"],
    oneLiner: "A rope stores long text as many smaller chunks so edits avoid copying the whole string.",
    body: "Instead of one giant contiguous string, a rope uses a tree whose leaves hold text pieces. Concatenation, insertion, and deletion can update only a few nodes rather than rebuilding everything. This makes it attractive for large text buffers and editors.",
    example: "A code editor can insert text near the top of a huge file without reallocating the entire file contents.",
    whyItMatters: "Ropes solve the practical problem that plain strings are expensive to edit repeatedly when documents become large.",
    relatedTerms: ["string", "balanced tree", "piece table", "text editor"]
  },
  {
    id: "persistent-data-structure",
    term: "Persistent Data Structure",
    category: "data-structures",
    tags: ["versioning", "functional", "sharing"],
    oneLiner: "A persistent data structure keeps old versions alive instead of overwriting them.",
    body: "When you update it, you get a new version while previous versions still remain usable. Efficient implementations share most unchanged parts between versions rather than copying everything. Persistence can be partial, where only the newest version is editable, or full, where all versions are.",
    example: "An undo system can jump back to older versions of a document without replaying every edit from scratch.",
    whyItMatters: "Persistence is useful for undo, snapshots, time travel debugging, and functional programming models where old state must stay valid.",
    relatedTerms: ["immutable data structure", "structural sharing", "snapshot", "versioning"]
  },
  {
    id: "immutable-data-structure",
    term: "Immutable Data Structure",
    category: "data-structures",
    tags: ["functional", "state", "safety"],
    oneLiner: "An immutable structure never changes in place - updates produce a new value instead.",
    body: "Because existing versions are never mutated, code can share them safely without worrying that another part of the program will change them underneath you. Efficient immutable structures usually rely on structural sharing so the new version reuses most of the old one. This is a design choice about how state behaves, not just a frozen object.",
    whyItMatters: "Immutable structures make reasoning about state easier, especially in concurrent or UI-heavy code where accidental mutation causes subtle bugs.",
    relatedTerms: ["persistent data structure", "structural sharing", "functional programming", "copy-on-write"]
  },
  {
    id: "crdt",
    term: "Conflict-free Replicated Data Type (CRDT)",
    category: "data-structures",
    tags: ["distributed-systems", "replication", "collaboration"],
    oneLiner: "A CRDT lets multiple replicas update shared data independently and still converge on the same result.",
    body: "Each replica can accept local changes without locking a central server first. The data type is designed so that when replicas later exchange updates, merges are deterministic and preserve a valid combined state. Different CRDTs handle counters, sets, text, and maps in different ways.",
    example: "A collaborative notes app can let two users edit offline and merge their changes later without manual conflict resolution for the underlying data model.",
    whyItMatters: "CRDTs are important when systems need offline edits, local responsiveness, and reliable merge behavior across many replicas.",
    relatedTerms: ["vector clock", "replication", "eventual consistency", "merge"]
  },
  {
    id: "merkle-tree",
    term: "Merkle Tree",
    category: "data-structures",
    tags: ["hashing", "verification", "distributed-systems"],
    oneLiner: "A Merkle tree hashes data upward so one top hash can summarize and verify a whole set of data.",
    body: "Leaves contain hashes of individual blocks, and each internal node stores the hash of its children. If any block changes, the hashes on the path to the root change too. That makes it possible to verify membership or detect differences by checking only a small proof instead of the full dataset.",
    example: "Git-like systems can compare large snapshots quickly because matching Merkle roots imply matching subtrees.",
    whyItMatters: "Merkle trees are a core building block for distributed storage, blockchains, and any system that needs compact integrity proofs.",
    relatedTerms: ["hash function", "tree", "integrity", "blockchain"]
  },
  {
    id: "dag",
    term: "Directed Acyclic Graph (DAG)",
    category: "data-structures",
    tags: ["graph", "dependencies", "ordering"],
    oneLiner: "A DAG is a directed graph with no cycles, so dependencies always flow forward.",
    body: "Edges have direction, but you can never start at a node and follow arrows back to it. That property makes topological ordering possible because some nodes must come before others. DAGs are often used to model dependencies, pipelines, and shared history without loops.",
    example: "A build system can represent tasks as a DAG so source generation happens before compilation, and compilation happens before packaging.",
    whyItMatters: "DAGs are how many systems model work ordering, dependency resolution, and version history while avoiding impossible circular relationships.",
    relatedTerms: ["graph", "topological sort", "dependency graph", "adjacency list"]
  },
  {
    id: "adjacency-list-vs-matrix",
    term: "Adjacency List vs Adjacency Matrix",
    category: "data-structures",
    tags: ["graph", "tradeoffs", "representation"],
    oneLiner: "The right graph representation depends on whether memory savings or instant edge checks matter more.",
    body: "An adjacency list stores only the edges that exist, so it is usually better for sparse graphs and neighbor iteration. An adjacency matrix stores every possible connection slot, so it uses more space but makes edge existence checks direct. The best choice depends on graph density and the operations you run most often.",
    whyItMatters: "This tradeoff shapes both performance and memory use in graph-heavy code, so choosing the wrong representation can make an otherwise good algorithm feel slow.",
    relatedTerms: ["adjacency list", "adjacency matrix", "graph", "sparse graph"]
  },
  {
    id: "lock-free-data-structure",
    term: "Lock-Free Data Structure",
    category: "data-structures",
    tags: ["concurrency", "non-blocking", "performance"],
    oneLiner: "A lock-free structure lets threads make progress without waiting on a traditional mutex.",
    body: "Instead of guarding the whole structure with a lock, it uses atomic operations such as compare-and-swap to coordinate updates. Individual operations may retry when they collide, but the system guarantees that some thread always makes progress. Designing them correctly is hard because memory ordering and edge cases matter a lot.",
    whyItMatters: "Lock-free structures can reduce contention and latency in highly concurrent systems where blocking on a shared lock becomes a bottleneck.",
    relatedTerms: ["concurrency", "atomic operation", "compare-and-swap", "wait-free"]
  },
  {
    id: "vector-clock",
    term: "Vector Clock",
    category: "data-structures",
    tags: ["distributed-systems", "ordering", "metadata"],
    oneLiner: "A vector clock tracks what each replica has seen so systems can compare event histories.",
    body: "It keeps a counter per participant, and each participant increments its own counter when it creates an event. By comparing vectors, a system can tell whether one event happened before another or whether two events are concurrent. That makes it more informative than a single timestamp in distributed settings.",
    example: "Two offline devices can attach vector clocks to edits so the sync layer can tell whether one edit supersedes the other or whether they happened independently.",
    whyItMatters: "Vector clocks help distributed systems reason about causality, conflict detection, and merge decisions without relying on perfectly synchronized clocks.",
    relatedTerms: ["crdt", "logical clock", "causality", "distributed systems"]
  },
  {
    id: "hyperloglog",
    term: "HyperLogLog",
    category: "data-structures",
    tags: ["probabilistic", "counting", "analytics"],
    oneLiner: "HyperLogLog estimates how many distinct items you saw without storing them all.",
    body: "It hashes incoming values and keeps a tiny summary based on patterns in the hash bits. That summary is enough to estimate cardinality with a small, predictable error rate using very little memory. It is approximate, but the memory savings are enormous at scale.",
    example: "An analytics system can estimate daily unique visitors without storing every visitor ID in a giant set.",
    whyItMatters: "HyperLogLog is a practical answer to large-scale distinct counting, where exact sets are often too expensive in memory.",
    relatedTerms: ["count-min sketch", "bloom filter", "cardinality", "probabilistic data structure"]
  },
  {
    id: "count-min-sketch",
    term: "Count-Min Sketch",
    category: "data-structures",
    tags: ["probabilistic", "frequency", "streaming"],
    oneLiner: "A count-min sketch estimates how often items appear in a stream using a small fixed table.",
    body: "It hashes each item into several rows and increments the corresponding counters. To estimate an item's frequency, it checks those same counters and takes the minimum, which limits overcounting but does not remove it. The result is approximate, yet the structure stays compact even on huge streams.",
    example: "A service can estimate which search queries are trending without keeping an exact count for every rare query ever seen.",
    whyItMatters: "Count-min sketch is useful when you need rough frequency counts from high-volume streams and exact counting would cost too much memory.",
    relatedTerms: ["hyperloglog", "stream processing", "frequency estimation", "probabilistic data structure"]
  },
  {
    id: "quadtree",
    term: "Quadtree",
    category: "data-structures",
    tags: ["spatial", "index", "geometry"],
    oneLiner: "A quadtree divides two-dimensional space into four regions at a time.",
    body: "Each node represents a rectangular area and can split into four children when it gets too crowded or needs more detail. Sparse areas stay coarse, while busy areas subdivide more deeply. That adaptive layout makes spatial lookup more efficient than scanning every point.",
    example: "A map app can use a quadtree to find objects near the cursor without checking every object on the screen.",
    whyItMatters: "Quadtrees are a common way to organize 2D spatial data for collision checks, maps, and viewport queries.",
    relatedTerms: ["r-tree", "k-d tree", "spatial index", "geometry"]
  },
  {
    id: "r-tree",
    term: "R-Tree",
    category: "data-structures",
    tags: ["spatial", "index", "database"],
    oneLiner: "An R-tree groups nearby geometric objects inside nested bounding boxes.",
    body: "Each node stores rectangles that enclose its children, and searches descend only into boxes that overlap the query region. This works well for rectangles, polygons, and map features, not just points. The goal is to prune large parts of space quickly during spatial searches.",
    example: "A geospatial database can use an R-tree to find all roads, parcels, or stores inside a map window.",
    whyItMatters: "R-trees are a core spatial index in mapping and database systems because they make region and overlap queries practical.",
    relatedTerms: ["quadtree", "spatial index", "bounding box", "geospatial query"]
  },
  {
    id: "lsm-tree",
    term: "LSM Tree (Log-Structured Merge Tree)",
    category: "data-structures",
    tags: ["database", "storage", "write-optimized"],
    oneLiner: "An LSM tree is built for heavy writes by batching data in memory and merging it later on disk.",
    body: "New writes first land in memory and are flushed out as sorted files. Over time, background compaction merges those files into larger, cleaner levels. Reads may need to check several places, but the design greatly reduces random disk writes.",
    example: "Many modern key-value stores write to an in-memory table and later compact immutable on-disk runs into larger sorted files.",
    whyItMatters: "LSM trees power write-heavy databases because they trade some read complexity for much better sustained write performance on storage.",
    relatedTerms: ["b-tree", "sstable", "compaction", "database"]
  },
  {
    id: "k-d-tree",
    term: "K-D Tree",
    category: "data-structures",
    tags: ["spatial", "multidimensional", "search"],
    oneLiner: "A k-d tree organizes points in multiple dimensions by splitting space one axis at a time.",
    body: "Each level chooses a dimension and partitions points based on a value along that axis. Repeating that process creates a tree that can narrow down nearest-neighbor or range searches without checking every point. It works best for moderate-dimensional data rather than extremely high-dimensional spaces.",
    example: "A photo app can store image embeddings or GPS points in a k-d tree to find nearby matches faster than a full scan.",
    whyItMatters: "K-d trees are a useful spatial and geometric index when queries depend on distance or multi-axis ranges instead of simple equality.",
    relatedTerms: ["quadtree", "r-tree", "nearest neighbor", "spatial index"]
  }
];
