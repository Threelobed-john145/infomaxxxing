import { Concept } from '@/lib/types';

export const concurrencyConcepts: Concept[] = [
  {
    id: "semaphore",
    term: "Semaphore",
    category: "concurrency",
    tags: ["synchronization", "locking", "threads"],
    oneLiner: "A semaphore controls how many workers can enter a critical section at the same time.",
    body: "A semaphore holds a counter that represents how many permits are available. A counting semaphore allows more than one entrant, while a binary semaphore only allows zero or one. Threads or tasks acquire a permit before proceeding and release it when they finish.",
    example: "A download service might use a counting semaphore set to 10 so only 10 files are fetched at once.",
    whyItMatters: "Semaphores are a simple way to cap concurrency and protect scarce resources without forcing everything through a single lock.",
    relatedTerms: ["mutex", "condition variable", "producer-consumer pattern", "barrier synchronization"],
  },
  {
    id: "read-write-lock",
    term: "Read-Write Lock (RWLock)",
    category: "concurrency",
    tags: ["locking", "reads", "writes"],
    oneLiner: "A read-write lock lets many readers in together but gives writers exclusive access.",
    body: "RWLocks split access into shared reads and exclusive writes. Multiple readers can hold the lock at once when no writer is active. A writer must wait until all readers leave, which can improve throughput on read-heavy workloads.",
    example: "An in-memory cache may allow many concurrent reads, but only one thread can update an entry at a time.",
    whyItMatters: "This pattern can reduce needless blocking when reads are common, but it also introduces fairness and starvation trade-offs you need to understand.",
    relatedTerms: ["mutex", "reader-writer problem", "starvation", "monitor pattern"],
  },
  {
    id: "spinlock-vs-mutex",
    term: "Spinlock vs Mutex Trade-offs",
    category: "concurrency",
    tags: ["locking", "performance", "threads"],
    oneLiner: "Spinlocks burn CPU while waiting, while mutexes usually put the waiter to sleep.",
    body: "A spinlock keeps checking in a tight loop until the lock becomes free. A mutex typically asks the scheduler to block the waiting thread and wake it later. Spinlocks can win for extremely short waits, but they waste CPU if contention lasts long enough.",
    example: "Kernel code may use a spinlock around a tiny critical section where sleeping is not allowed.",
    whyItMatters: "Picking the wrong lock can turn a fast path into a CPU heater or a low-latency path into a context-switch bottleneck.",
    relatedTerms: ["mutex", "semaphore", "atomic operation", "priority inversion"],
  },
  {
    id: "lock-free-vs-wait-free",
    term: "Lock-Free vs Wait-Free Algorithms",
    category: "concurrency",
    tags: ["non-blocking", "algorithms", "performance"],
    oneLiner: "Both avoid locks, but wait-free guarantees per-thread progress while lock-free only guarantees system-wide progress.",
    body: "A lock-free algorithm ensures that some thread always makes progress, even under contention. A wait-free algorithm goes further and guarantees every thread finishes its operation in a bounded number of steps. Wait-free designs are stronger but usually harder to build and reason about.",
    whyItMatters: "These ideas explain why some concurrent structures scale well under contention and why proving their correctness is difficult.",
    relatedTerms: ["compare-and-swap", "atomic operation", "memory ordering", "mutex"],
  },
  {
    id: "compare-and-swap",
    term: "Compare-and-Swap (CAS)",
    category: "concurrency",
    tags: ["atomics", "lock-free", "hardware"],
    oneLiner: "CAS updates a value only if it still matches what you expected.",
    body: "Compare-and-swap reads a memory location, compares it to an expected value, and writes a new value if they match - all as one atomic step. If another thread changed the value first, the operation fails and the caller usually retries. Many lock-free algorithms are built on that retry loop.",
    example: "A lock-free stack may use CAS to replace the head pointer only if no other push or pop changed it first.",
    whyItMatters: "CAS is one of the core hardware primitives behind modern concurrent data structures and atomic libraries.",
    relatedTerms: ["atomic operation", "lock-free vs wait-free", "memory ordering", "aba problem"],
  },
  {
    id: "atomic-operations",
    term: "Atomic Operations",
    category: "concurrency",
    tags: ["atomics", "hardware", "synchronization"],
    oneLiner: "An atomic operation happens as one indivisible step from the perspective of other threads.",
    body: "Atomic reads, writes, increments, and exchanges cannot be observed halfway through by another thread. They are smaller and cheaper than many lock-based operations, but they do not automatically make larger workflows safe. You still need to reason about ordering and visibility.",
    example: "An atomic counter can track how many requests are in flight without wrapping every increment in a lock.",
    whyItMatters: "Atomics are the foundation for low-level synchronization, but misuse leads to subtle bugs that are hard to reproduce.",
    relatedTerms: ["compare-and-swap", "memory ordering", "memory barrier", "race condition"],
  },
  {
    id: "memory-ordering",
    term: "Memory Ordering",
    category: "concurrency",
    tags: ["memory-model", "atomics", "hardware"],
    oneLiner: "Memory ordering rules define when one thread is allowed to see another thread's reads and writes.",
    body: "Modern compilers and CPUs can reorder operations as long as single-threaded behavior stays correct. Acquire and release ordering create a handoff point between threads, while sequential consistency gives the simplest mental model by acting like operations happen in one global order. Weaker orderings can improve speed but make correctness harder to prove.",
    example: "A producer can publish data, then set a ready flag with release semantics; a consumer reads the flag with acquire semantics before using the data.",
    whyItMatters: "Without the right ordering, a program can look correct in code and still fail on real hardware because threads observe changes in the wrong order.",
    relatedTerms: ["atomic operation", "memory barrier", "volatile keyword", "compare-and-swap"],
  },
  {
    id: "memory-barriers-and-fences",
    term: "Memory Barriers and Fences",
    category: "concurrency",
    tags: ["memory-model", "hardware", "synchronization"],
    oneLiner: "Memory barriers stop certain reads and writes from being reordered across a boundary.",
    body: "A memory barrier, also called a fence, tells the compiler or CPU that operations on one side must become visible before operations on the other side. Fences are lower-level tools than ordinary locks. They are often used inside runtime libraries, lock-free data structures, and atomic primitives.",
    whyItMatters: "Fences are part of the machinery that makes higher-level synchronization work, and they matter when you are close to the hardware or debugging visibility bugs.",
    relatedTerms: ["memory ordering", "atomic operation", "compare-and-swap", "volatile keyword"],
  },
  {
    id: "volatile-keyword",
    term: "Volatile Keyword",
    category: "concurrency",
    tags: ["java", "c-plus-plus", "memory-model"],
    oneLiner: "The meaning of `volatile` depends on the language, which makes it easy to misuse.",
    body: "In Java, `volatile` gives visibility guarantees so reads see the latest write and it prevents some reorderings around that variable. In C and C++, `volatile` mostly means the value may change outside normal program flow, such as memory-mapped hardware, and it is not a general threading tool. Treating the two meanings as interchangeable causes bugs.",
    example: "A Java shutdown flag may be `volatile`; the same idea in modern C++ usually needs `std::atomic<bool>` instead.",
    whyItMatters: "Many concurrency mistakes come from assuming `volatile` magically makes shared state safe when it usually does not.",
    relatedTerms: ["memory ordering", "atomic operation", "memory barrier", "race condition"],
  },
  {
    id: "thread-pool-pattern",
    term: "Thread Pool Pattern",
    category: "concurrency",
    tags: ["threads", "scheduling", "performance"],
    oneLiner: "A thread pool reuses a fixed or managed set of workers instead of creating a new thread for every task.",
    body: "Tasks are submitted to a queue and picked up by long-lived worker threads. Reuse avoids the cost of repeatedly creating and destroying threads. Pools also give you a place to cap parallel work and apply backpressure.",
    example: "A web server might hand incoming jobs to a pool of worker threads rather than spawning a fresh thread per request.",
    whyItMatters: "Thread pools are one of the most common ways to control throughput, latency, and resource usage in server software.",
    relatedTerms: ["work stealing", "producer-consumer pattern", "backpressure", "green threads fibers and virtual threads"],
  },
  {
    id: "work-stealing",
    term: "Work Stealing",
    category: "concurrency",
    tags: ["scheduling", "fork-join", "performance"],
    oneLiner: "Work stealing keeps workers busy by letting idle ones pull tasks from busier workers.",
    body: "Instead of using a single shared queue, each worker often keeps its own deque of tasks. When a worker runs out of work, it steals from another worker's queue. This reduces contention and helps balance uneven workloads in fork-join systems.",
    example: "Recursive parallel algorithms often split tasks locally and let idle workers steal unfinished branches.",
    whyItMatters: "This scheduling strategy is a big reason modern task runtimes scale better than naive thread pools on irregular workloads.",
    relatedTerms: ["thread pool pattern", "actor model", "parallelism", "fork-join"],
  },
  {
    id: "producer-consumer-pattern",
    term: "Producer-Consumer Pattern",
    category: "concurrency",
    tags: ["queues", "coordination", "threads"],
    oneLiner: "Producers create work and consumers process it, usually with a shared buffer in between.",
    body: "The shared buffer decouples the rate of production from the rate of consumption. Producers may block when the buffer is full, and consumers may block when it is empty. Queues, semaphores, and condition variables are common building blocks for this pattern.",
    example: "One thread reads log lines from the network and pushes them into a queue while another thread parses and stores them.",
    whyItMatters: "This pattern shows up everywhere from job systems to streaming pipelines and is the basis for many backpressure problems.",
    relatedTerms: ["semaphore", "condition variable", "backpressure", "thread pool pattern"],
  },
  {
    id: "reader-writer-problem",
    term: "Reader-Writer Problem",
    category: "concurrency",
    tags: ["locking", "fairness", "synchronization"],
    oneLiner: "The reader-writer problem asks how to allow concurrent reads without starving writers or readers.",
    body: "Readers can safely share access when no one is writing, but writers need exclusivity. Different solutions prioritize readers, writers, or fairness. Each choice changes throughput and the risk that one side waits forever.",
    whyItMatters: "This classic problem explains why real read-write locks need explicit fairness policies instead of just shared and exclusive modes.",
    relatedTerms: ["read-write lock", "starvation", "monitor pattern", "condition variable"],
  },
  {
    id: "dining-philosophers-problem",
    term: "Dining Philosophers Problem",
    category: "concurrency",
    tags: ["deadlock", "coordination", "theory"],
    oneLiner: "The dining philosophers problem is a classic model for resource contention and deadlock.",
    body: "Several philosophers sit around a table with one fork between each pair, and each needs two forks to eat. If everyone grabs one fork first, nobody can proceed. The problem is simple to state but exposes deadlock, starvation, and coordination strategy trade-offs.",
    whyItMatters: "It gives a memorable way to reason about real systems that compete for multiple shared resources at once.",
    relatedTerms: ["deadlock", "livelock vs deadlock vs starvation", "semaphore", "monitor pattern"],
  },
  {
    id: "actor-model",
    term: "Actor Model",
    category: "concurrency",
    tags: ["message-passing", "distributed-systems", "isolation"],
    oneLiner: "The actor model structures concurrency around isolated workers that communicate by sending messages.",
    body: "Each actor owns its own state and processes one message at a time. Instead of sharing memory, actors coordinate by exchanging messages, spawning other actors, and changing behavior. Systems like Erlang and Akka use this model to make highly concurrent programs easier to reason about.",
    example: "A chat service can give each room its own actor that serializes membership and message delivery.",
    whyItMatters: "Actors reduce shared-state bugs and map well to fault-tolerant, distributed, and high-concurrency workloads.",
    relatedTerms: ["communicating sequential processes", "event-driven concurrency", "thread-local storage", "message queue"],
  },
  {
    id: "communicating-sequential-processes",
    term: "Communicating Sequential Processes (CSP)",
    category: "concurrency",
    tags: ["channels", "go", "message-passing"],
    oneLiner: "CSP treats concurrent work as independent processes that coordinate through channels instead of shared memory.",
    body: "In a CSP-style system, processes do not reach into each other's state directly. They send values through channels and synchronize at communication points. Go popularized this style with goroutines and channels.",
    example: "A Go pipeline might send parsed records from one stage to the next over typed channels.",
    whyItMatters: "CSP offers a clean alternative to lock-heavy designs and encourages clear data flow between concurrent tasks.",
    relatedTerms: ["actor model", "producer-consumer pattern", "coroutines", "event-driven concurrency"],
  },
  {
    id: "software-transactional-memory",
    term: "Software Transactional Memory (STM)",
    category: "concurrency",
    tags: ["transactions", "state-management", "functional-programming"],
    oneLiner: "STM lets concurrent code update shared state as if it were using small in-memory transactions.",
    body: "Code runs inside a transaction that reads and writes shared values optimistically. If another transaction conflicts, the runtime retries one of them. The goal is to replace explicit lock choreography with composable transactional rules.",
    whyItMatters: "STM can make certain kinds of concurrent state updates easier to compose correctly, especially when multiple locks would otherwise interact badly.",
    relatedTerms: ["acid", "lock-free vs wait-free", "monitor pattern", "compare-and-swap"],
  },
  {
    id: "futures-and-promises",
    term: "Futures and Promises",
    category: "concurrency",
    tags: ["async", "coordination", "programming"],
    oneLiner: "A future represents a result that will exist later, and a promise is the handle that fulfills it.",
    body: "These abstractions let code start work now and collect the result later. Futures can be composed, awaited, or chained, depending on the language. Promises usually represent the producer side that resolves or rejects the pending result.",
    example: "A function may return a future for a database query while the caller continues doing other work.",
    whyItMatters: "They are a common bridge between thread-based, task-based, and event-loop-based concurrency models.",
    relatedTerms: ["async-await", "coroutines", "reactive programming", "thread pool pattern"],
  },
  {
    id: "reactive-programming",
    term: "Reactive Programming",
    category: "concurrency",
    tags: ["streams", "events", "async"],
    oneLiner: "Reactive programming models values as streams that change over time and can be transformed declaratively.",
    body: "Instead of pulling values one by one, code subscribes to streams and reacts when new items arrive. Operators like map, filter, merge, and debounce describe how streams combine and evolve. Libraries such as RxJS and Reactor make this style popular in UI and server systems.",
    example: "A search box can turn keystrokes into a stream, debounce them, cancel stale requests, and display the latest result.",
    whyItMatters: "Reactive systems help structure asynchronous flows, but they introduce their own concurrency rules around scheduling, cancellation, and pressure.",
    relatedTerms: ["backpressure", "event-driven concurrency", "futures and promises", "observer"],
  },
  {
    id: "backpressure",
    term: "Backpressure",
    category: "concurrency",
    tags: ["streams", "flow-control", "performance"],
    oneLiner: "Backpressure is how a system tells producers to slow down when consumers cannot keep up.",
    body: "Without backpressure, buffers grow, latency spikes, and systems can run out of memory. Reactive streams frameworks often build explicit demand signaling into the protocol. Other systems use bounded queues, dropped work, or rate limits to achieve the same effect.",
    example: "If a stream consumer only requests 100 items at a time, the producer should not flood it with a million items immediately.",
    whyItMatters: "Many concurrency failures are really overload problems, and backpressure is one of the main tools for keeping a system stable.",
    relatedTerms: ["producer-consumer pattern", "reactive programming", "thread pool pattern", "rate limiting"],
  },
  {
    id: "cooperative-vs-preemptive-multitasking",
    term: "Cooperative vs Preemptive Multitasking",
    category: "concurrency",
    tags: ["scheduling", "runtime", "operating-systems"],
    oneLiner: "Cooperative systems yield voluntarily, while preemptive systems can interrupt a task and switch away from it.",
    body: "In cooperative multitasking, code must reach a yield point before something else can run. In preemptive multitasking, the scheduler can pause a running thread to give time to another. Cooperative models can be simpler and cheaper, but one badly behaved task can stall the whole system.",
    whyItMatters: "This distinction shapes latency, fairness, and failure modes in everything from event loops to operating systems.",
    relatedTerms: ["coroutines", "green threads fibers and virtual threads", "event loop", "thread pool pattern"],
  },
  {
    id: "green-threads-fibers-and-virtual-threads",
    term: "Green Threads, Fibers, and Virtual Threads",
    category: "concurrency",
    tags: ["runtime", "threads", "scheduling"],
    oneLiner: "These are lightweight threads managed mostly by a runtime instead of mapping one-to-one to operating system threads.",
    body: "Because they are cheaper to create and park, runtimes can support huge numbers of concurrent tasks this way. Fibers and virtual threads still run on a smaller pool of underlying system threads. Java Loom is a modern example that brings this style back into mainstream server programming.",
    example: "A service can handle many blocked network requests with virtual threads without dedicating a full OS thread to each one.",
    whyItMatters: "Lightweight threads change the cost model of blocking code and can simplify programs that were previously forced into callback-heavy designs.",
    relatedTerms: ["cooperative vs preemptive multitasking", "coroutines", "thread pool pattern", "async-await"],
  },
  {
    id: "coroutines",
    term: "Coroutines",
    category: "concurrency",
    tags: ["async", "language-features", "runtime"],
    oneLiner: "Coroutines are functions that can pause and resume while keeping their local state intact.",
    body: "A coroutine can suspend at well-defined points and continue later without starting over. Languages like Kotlin and Python use coroutines to make asynchronous work feel linear. They are usually scheduled cooperatively by a runtime or event loop rather than by the operating system.",
    example: "A coroutine may suspend while waiting on network I/O and resume with the response when it arrives.",
    whyItMatters: "Coroutines are a core abstraction behind modern async code because they make large concurrent workflows easier to write and follow.",
    relatedTerms: ["async-await", "green threads fibers and virtual threads", "futures and promises", "cooperative vs preemptive multitasking"],
  },
  {
    id: "event-driven-concurrency",
    term: "Event-Driven Concurrency",
    category: "concurrency",
    tags: ["events", "async", "systems"],
    oneLiner: "Event-driven concurrency handles many tasks by reacting to incoming events instead of dedicating a thread to each one.",
    body: "An event loop waits for work such as network readiness, timers, or messages, then dispatches handlers. This model is especially effective for I/O-heavy systems where most tasks spend time waiting. It trades simple blocking code for explicit callbacks, promises, or coroutines.",
    example: "Node.js can keep thousands of sockets open because the main thread mostly waits for readiness events.",
    whyItMatters: "This model is a major reason high-concurrency servers can stay efficient without creating massive numbers of operating system threads.",
    relatedTerms: ["event loop", "reactive programming", "coroutines", "actor model"],
  },
  {
    id: "thread-local-storage",
    term: "Thread-Local Storage",
    category: "concurrency",
    tags: ["threads", "state", "runtime"],
    oneLiner: "Thread-local storage gives each thread its own private copy of a variable.",
    body: "A thread-local value looks global from inside the thread, but other threads see different copies. This avoids sharing for things like request context, scratch buffers, or database handles. It simplifies some designs, but it can hide dependencies and break down when work hops between threads.",
    example: "A logging library may store the current request ID in thread-local storage so log lines include it automatically.",
    whyItMatters: "Thread-local storage can reduce synchronization, but it also creates subtle coupling to a scheduling model that may change later.",
    relatedTerms: ["green threads fibers and virtual threads", "actor model", "monitor pattern", "context propagation"],
  },
  {
    id: "barrier-synchronization",
    term: "Barrier Synchronization",
    category: "concurrency",
    tags: ["coordination", "threads", "parallelism"],
    oneLiner: "A barrier makes a group of workers wait until everyone reaches the same checkpoint.",
    body: "No participant can move past the barrier until the full group arrives. This is useful in phased algorithms where one stage must finish everywhere before the next stage begins. Some barriers are reusable across multiple rounds of work.",
    example: "In a simulation, all worker threads may compute the next step in parallel and then wait at a barrier before applying it.",
    whyItMatters: "Barriers are essential when correctness depends on workers advancing in lockstep rather than independently.",
    relatedTerms: ["condition variable", "semaphore", "thread pool pattern", "parallelism"],
  },
  {
    id: "condition-variables",
    term: "Condition Variables",
    category: "concurrency",
    tags: ["synchronization", "waiting", "locking"],
    oneLiner: "A condition variable lets threads sleep until a specific state becomes true.",
    body: "A thread usually waits on the condition while holding a lock, then the runtime atomically releases the lock and parks the thread. Another thread changes shared state, signals the condition, and wakes waiters. Because wakeups can be missed or spurious, the condition must be rechecked in a loop.",
    example: "A consumer waits until a queue becomes non-empty, then wakes up and removes an item.",
    whyItMatters: "Condition variables are a core tool for efficient coordination because they avoid wasteful busy-waiting.",
    relatedTerms: ["monitor pattern", "semaphore", "producer-consumer pattern", "read-write lock"],
  },
  {
    id: "monitor-pattern",
    term: "Monitor Pattern",
    category: "concurrency",
    tags: ["locking", "encapsulation", "synchronization"],
    oneLiner: "A monitor bundles shared state, the lock protecting it, and the rules for waiting and signaling.",
    body: "Instead of exposing raw shared data and separate synchronization primitives, a monitor hides them behind methods. Only one thread runs inside the monitor at a time, and condition variables coordinate state changes. This creates a disciplined way to protect invariants.",
    example: "A bounded buffer object can expose `put` and `take` methods while internally managing its lock and wait conditions.",
    whyItMatters: "The monitor pattern reduces synchronization mistakes by keeping the data and the coordination logic in one place.",
    relatedTerms: ["condition variable", "mutex", "reader-writer problem", "producer-consumer pattern"],
  },
  {
    id: "livelock-vs-deadlock-vs-starvation",
    term: "Livelock vs Deadlock vs Starvation",
    category: "concurrency",
    tags: ["failure-modes", "locking", "fairness"],
    oneLiner: "These are three different ways concurrent work can fail to make progress.",
    body: "In deadlock, tasks wait forever on each other and nothing moves. In livelock, tasks keep reacting and changing state, but still never complete useful work. In starvation, one task could proceed in theory but keeps getting denied the chance because others always win.",
    example: "Two polite threads that keep backing off for each other can livelock even though neither is technically blocked.",
    whyItMatters: "You need to distinguish these failure modes because they come from different design mistakes and require different fixes.",
    relatedTerms: ["deadlock", "race condition", "priority inversion", "reader-writer problem"],
  },
  {
    id: "priority-inversion",
    term: "Priority Inversion",
    category: "concurrency",
    tags: ["scheduling", "locks", "real-time"],
    oneLiner: "Priority inversion happens when a high-priority task is forced to wait behind a lower-priority one.",
    body: "The classic case is a low-priority task holding a lock that a high-priority task needs. If a medium-priority task keeps preempting the low-priority one, the high-priority task waits far longer than expected. Systems may use priority inheritance to temporarily boost the lock holder and reduce the problem.",
    example: "A real-time audio thread can glitch if it blocks on a lock held by a low-priority background worker.",
    whyItMatters: "Priority inversion turns a seemingly correct synchronization design into a latency problem, especially in real-time and interactive systems.",
    relatedTerms: ["spinlock vs mutex", "livelock vs deadlock vs starvation", "thread pool pattern", "cooperative vs preemptive multitasking"],
  },
];
