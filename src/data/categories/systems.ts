import { Concept } from '@/lib/types';

export const systemsConcepts: Concept[] = [
  {
    id: "process-thread-coroutine",
    term: "Process, Thread, and Coroutine",
    category: "systems",
    tags: ["execution", "os", "concurrency"],
    oneLiner: "They are three different ways to structure work, with different isolation and scheduling costs.",
    body: "A process is an isolated program with its own address space and operating system resources. A thread runs inside a process and shares that process's memory, which makes communication cheaper but coordination riskier. A coroutine is a lighter unit of work managed mostly by a runtime, usually yielding cooperatively instead of being preempted by the kernel.",
    example: "A browser might run each tab as a process, do rendering and networking on separate threads, and use coroutines inside an async runtime to handle thousands of open sockets.",
    whyItMatters: "Choosing the wrong unit of execution can waste memory, limit throughput, or create hard-to-debug synchronization bugs.",
    relatedTerms: ["context switch", "kernel vs user space", "async runtime", "scheduler"]
  },
  {
    id: "virtual-memory",
    term: "Virtual Memory",
    category: "systems",
    tags: ["memory", "os", "hardware"],
    oneLiner: "Virtual memory gives each process the illusion of a large private memory space.",
    body: "Programs do not work with raw physical RAM addresses directly. The operating system and CPU translate virtual addresses to physical ones, which lets processes stay isolated from each other and lets memory be moved around safely. This also makes features like memory protection, shared libraries, and swapping possible.",
    example: "Two processes can both think they are using address `0x1000`, but the hardware maps those virtual addresses to different physical pages.",
    whyItMatters: "A lot of performance and correctness behavior in systems work comes from how virtual memory is mapped, protected, and faulted in.",
    relatedTerms: ["paging", "page fault", "kernel vs user space", "memory protection"]
  },
  {
    id: "paging",
    term: "Paging",
    category: "systems",
    tags: ["memory", "os", "hardware"],
    oneLiner: "Paging splits memory into fixed-size pages so virtual memory can be mapped in pieces instead of as one giant block.",
    body: "The CPU looks up a virtual page number in page tables to find the corresponding physical page frame. This design avoids requiring contiguous physical memory and lets the operating system move pages between RAM and disk. It is the core mechanism behind most modern virtual memory systems.",
    example: "A 4 KB page containing a function's code can be loaded into RAM even if the pages before and after it are somewhere else entirely.",
    whyItMatters: "Understanding paging explains why memory access is not free and why page table layout and translation caches matter for performance.",
    relatedTerms: ["virtual memory", "page fault", "tlb", "swap"]
  },
  {
    id: "page-fault",
    term: "Page Fault",
    category: "systems",
    tags: ["memory", "os", "performance"],
    oneLiner: "A page fault happens when a process touches a virtual page that is not currently mapped the way the CPU expects.",
    body: "Some page faults are normal, such as the first access to a lazily loaded page or a copy-on-write page. Others are fatal, such as touching unmapped memory or violating permissions. Handling a fault may require the kernel to load data from disk, update page tables, and resume the instruction, which can be very expensive.",
    example: "A program opens a huge file with memory mapping and only pays the cost for each page when it actually reads from that portion of the file.",
    whyItMatters: "A small number of faults is normal, but frequent major faults can destroy latency and make a service look randomly slow.",
    relatedTerms: ["virtual memory", "paging", "copy-on-write", "memory-mapped i/o"]
  },
  {
    id: "system-call",
    term: "System Call",
    category: "systems",
    tags: ["os", "kernel", "interfaces"],
    oneLiner: "A system call is the controlled gateway from application code into kernel services.",
    body: "User programs cannot directly perform privileged operations like reading files, creating sockets, or changing page tables. Instead they ask the kernel to do that work through system calls such as `read`, `write`, `open`, and `fork`. Crossing that boundary adds overhead because control moves into the kernel and back out again.",
    example: "When your app writes a line to a log file, it eventually reaches a `write` system call that asks the kernel to send those bytes to the file system.",
    whyItMatters: "System call frequency often becomes a hidden bottleneck in I/O-heavy software, and understanding the boundary helps when profiling or debugging permissions problems.",
    relatedTerms: ["kernel vs user space", "interrupt", "file system", "context switch"]
  },
  {
    id: "kernel-vs-user-space",
    term: "Kernel vs User Space",
    category: "systems",
    tags: ["os", "security", "memory"],
    oneLiner: "Kernel space runs with full hardware privileges, while user space runs with restrictions for safety and isolation.",
    body: "The kernel controls memory, devices, scheduling, and security boundaries, so bugs there are especially dangerous. User space applications run with limited privileges and must request protected operations from the kernel. This split is what keeps one crashing app from taking down the whole machine in normal circumstances.",
    example: "A text editor can crash in user space without corrupting the disk driver, because the disk driver and page tables are managed in kernel space.",
    whyItMatters: "Many systems bugs come from misunderstanding which work happens in the kernel, which happens in user space, and what each boundary crossing costs.",
    relatedTerms: ["system call", "virtual memory", "process, thread, and coroutine", "privilege levels"]
  },
  {
    id: "file-systems",
    term: "File Systems",
    category: "systems",
    tags: ["storage", "os", "persistence"],
    oneLiner: "A file system decides how bytes on a storage device become files, directories, metadata, and recovery rules.",
    body: "Different file systems make different tradeoffs in integrity, features, and compatibility. ext4 is a common Linux default with journaling and solid general-purpose performance, NTFS is Windows' long-standing file system with rich metadata and permissions, and ZFS emphasizes data integrity, snapshots, and pooled storage. Underneath the friendly directory tree, the file system is managing allocation, naming, permissions, and crash recovery.",
    example: "A database on ext4 may rely on journaling and stable write ordering, while a backup appliance may choose ZFS for checksums and snapshots.",
    whyItMatters: "Storage behavior is shaped as much by the file system as by the disk itself, especially when you care about durability, corruption recovery, or write performance.",
    relatedTerms: ["write-ahead logging", "system call", "journaling", "copy-on-write"]
  },
  {
    id: "interrupt",
    term: "Interrupt",
    category: "systems",
    tags: ["hardware", "cpu", "os"],
    oneLiner: "An interrupt is a hardware or software signal that asks the CPU to stop its current path and handle something urgent.",
    body: "Devices use interrupts to notify the CPU that work has completed or attention is needed, such as a packet arriving or a timer expiring. The CPU saves enough state to run an interrupt handler and then resumes what it was doing. Without interrupts, the system would waste huge amounts of time polling devices to see if anything happened.",
    example: "A network card can interrupt the CPU to say a new packet is ready, instead of forcing the kernel to repeatedly ask the card whether data has arrived.",
    whyItMatters: "Interrupt behavior affects latency, throughput, and CPU overhead, especially in high-I/O systems.",
    relatedTerms: ["context switch", "direct memory access", "system call", "scheduler"]
  },
  {
    id: "context-switch",
    term: "Context Switch",
    category: "systems",
    tags: ["cpu", "scheduling", "performance"],
    oneLiner: "A context switch is the work required to pause one execution stream and resume another.",
    body: "The operating system must save registers, stack state, and scheduling metadata before it can run something else. Switching between processes is usually more expensive than switching between threads, and both are usually heavier than handing off between coroutines. Context switches make multitasking possible, but too many of them burn CPU on bookkeeping instead of useful work.",
    example: "A server with thousands of constantly waking threads may spend a surprising amount of time switching between them rather than processing requests.",
    whyItMatters: "High context-switch rates are a common sign that a system is overscheduled, heavily contended, or using the wrong concurrency model.",
    relatedTerms: ["process, thread, and coroutine", "interrupt", "scheduler", "false sharing"]
  },
  {
    id: "memory-mapped-io",
    term: "Memory-Mapped I/O",
    category: "systems",
    tags: ["memory", "io", "os"],
    oneLiner: "Memory mapping lets software treat files or device regions like memory instead of manually copying bytes through explicit read calls.",
    body: "With memory-mapped files, the kernel maps file contents into a process address space and loads pages on demand. With device mappings, certain address ranges correspond to hardware registers rather than normal RAM. In both cases, ordinary loads and stores can trigger I/O behavior under the hood.",
    example: "A database can map an index file and jump directly to a key's page instead of issuing lots of small `read` calls.",
    whyItMatters: "Memory mapping can simplify code and improve performance, but it also changes when I/O costs show up and how failures appear.",
    relatedTerms: ["page fault", "virtual memory", "direct memory access", "file systems"]
  },
  {
    id: "direct-memory-access",
    term: "Direct Memory Access (DMA)",
    category: "systems",
    tags: ["hardware", "io", "performance"],
    oneLiner: "DMA lets devices move data to or from memory without making the CPU copy every byte itself.",
    body: "The CPU sets up the transfer and the device or DMA controller performs the bulk movement. That frees the CPU to do other work while data is being moved between a device and RAM. DMA is central to efficient networking, storage, and graphics because software polling and copying would be too expensive at scale.",
    example: "A network card can write incoming packet data directly into main memory, then interrupt the CPU once a buffer is ready to process.",
    whyItMatters: "Many performance gains in systems programming come from reducing needless CPU copying, and DMA is one of the main ways hardware helps with that.",
    relatedTerms: ["interrupt", "memory-mapped i/o", "cpu cache hierarchy", "zero-copy"]
  },
  {
    id: "cpu-cache-hierarchy",
    term: "CPU Cache Hierarchy",
    category: "systems",
    tags: ["cpu", "memory", "performance"],
    oneLiner: "L1, L2, and L3 caches are small fast memory layers that hide the much higher latency of main RAM.",
    body: "L1 is the smallest and fastest, L2 is larger and slower, and L3 is larger still and often shared across cores. The CPU tries to keep recently used and nearby data in these caches so it does not have to wait on DRAM. Good locality means your program reuses data that is already close to the core instead of constantly missing the cache.",
    example: "A tight loop over a small array can run dramatically faster than the same loop over a huge scattered data set because the working set stays hot in cache.",
    whyItMatters: "A lot of real performance work is really cache work, even when the code looks mathematically simple.",
    relatedTerms: ["cache line", "false sharing", "numa", "memory latency"]
  },
  {
    id: "cache-line",
    term: "Cache Line",
    category: "systems",
    tags: ["cpu", "memory", "performance"],
    oneLiner: "A cache line is the fixed-size chunk of memory that the CPU cache moves around as a unit.",
    body: "Processors usually fetch and track memory in cache-line-sized blocks rather than one variable at a time. That is why touching one byte often brings neighboring bytes into cache too. It is also why data layout matters: nearby fields can help performance or accidentally create contention depending on access patterns.",
    example: "If a struct's hot fields fit within one cache line, a tight loop may need fewer memory fetches than if those fields spill across multiple lines.",
    whyItMatters: "Thinking at the cache-line level helps explain why layout changes can outperform algorithm tweaks in low-level performance work.",
    relatedTerms: ["cpu cache hierarchy", "false sharing", "data locality", "prefetching"]
  },
  {
    id: "false-sharing",
    term: "False Sharing",
    category: "systems",
    tags: ["cpu", "multicore", "performance"],
    oneLiner: "False sharing happens when different cores modify different variables that live on the same cache line.",
    body: "The variables are logically independent, but the hardware coherence protocol tracks them as one unit because they share a cache line. Each core keeps invalidating the other's line, so performance collapses even though no real shared state exists at the application level. Padding, alignment, or layout changes often fix it.",
    example: "Two worker threads each increment their own counter, but both counters were stored next to each other in memory and now the cores keep fighting over that line.",
    whyItMatters: "False sharing is a classic source of mysterious multicore slowdowns that do not show up in single-threaded benchmarks.",
    relatedTerms: ["cache line", "cpu cache hierarchy", "context switch", "numa"]
  },
  {
    id: "numa",
    term: "NUMA Architecture",
    category: "systems",
    tags: ["hardware", "memory", "multicore"],
    oneLiner: "NUMA means memory access time depends on which CPU socket owns the memory you are touching.",
    body: "In a non-uniform memory access machine, each socket has faster access to its local memory and slower access to remote memory attached to another socket. The system still presents one address space, but not all addresses are equally cheap. Thread placement and memory placement start to matter much more once workloads span sockets.",
    example: "A database thread running on socket A may slow down if its hot data pages were allocated on socket B and every read has to cross the interconnect.",
    whyItMatters: "Ignoring NUMA can make a machine with more hardware perform worse than a smaller one because remote memory penalties add up fast.",
    relatedTerms: ["cpu cache hierarchy", "false sharing", "scheduler", "memory locality"]
  },
  {
    id: "distributed-consensus",
    term: "Distributed Consensus",
    category: "systems",
    tags: ["distributed-systems", "coordination", "fault-tolerance"],
    oneLiner: "Consensus is how multiple machines agree on a shared sequence of decisions even when some of them fail.",
    body: "The hard part is not normal operation but disagreement under crashes, delays, retries, and partial partitions. Consensus algorithms are designed so nodes can still agree on who the leader is or what the next committed log entry should be. This is the foundation for strongly coordinated systems like metadata stores and control planes.",
    example: "A cluster of configuration servers needs every healthy node to agree on the same order of writes so clients do not read conflicting state.",
    whyItMatters: "If coordination is wrong, the rest of the distributed system can corrupt data or split into conflicting realities.",
    relatedTerms: ["raft and paxos", "leader election", "write-ahead logging", "quorum"]
  },
  {
    id: "raft-and-paxos",
    term: "Raft and Paxos",
    category: "systems",
    tags: ["distributed-systems", "consensus", "algorithms"],
    oneLiner: "Raft and Paxos solve consensus, but Raft is generally taught as the more understandable design while Paxos is the classic family of proofs and protocols.",
    body: "Paxos focuses on the rules required for proposals and quorums to converge safely, which makes it powerful but hard to reason about in practice. Raft organizes the problem around a leader, replicated log, and clearer role transitions, which made it popular for production systems and education. Both are about agreeing safely under failure, not about being fast in the common case at any cost.",
    example: "etcd and Consul are Raft-based, while Paxos ideas show up in systems like Chubby and in many academic treatments of distributed agreement.",
    whyItMatters: "You do not need to implement these often, but you do need to recognize when your system is quietly depending on one of them.",
    relatedTerms: ["distributed consensus", "leader election", "quorum", "replicated log"]
  },
  {
    id: "leader-election",
    term: "Leader Election",
    category: "systems",
    tags: ["distributed-systems", "coordination", "availability"],
    oneLiner: "Leader election is the process of deciding which node is currently allowed to coordinate a group.",
    body: "Many distributed systems avoid conflicting writes by having one node act as the leader for a period of time. If that node fails or becomes unreachable, the cluster must choose a replacement without accidentally allowing two leaders to exist at once. Good election rules trade off failover speed against the risk of split-brain behavior.",
    example: "A cluster may stop accepting writes briefly while it confirms the old leader is gone and elects a new one to own the replicated log.",
    whyItMatters: "A system that cannot elect a stable leader often looks flaky, stalls under failure, or serves contradictory answers.",
    relatedTerms: ["distributed consensus", "health checks and heartbeats", "quorum", "split brain"]
  },
  {
    id: "gossip-protocol",
    term: "Gossip Protocol",
    category: "systems",
    tags: ["distributed-systems", "membership", "coordination"],
    oneLiner: "A gossip protocol spreads information through a cluster the way rumors spread through a crowd.",
    body: "Each node periodically exchanges state with a small number of peers, and that information gradually propagates outward. Gossip is decentralized and resilient because there is no single coordinator that everyone depends on. It is commonly used for node membership, failure suspicion, and approximate cluster state.",
    example: "A node joining a cluster tells a few peers it exists, and within a short time the whole cluster learns about it through repeated peer-to-peer exchanges.",
    whyItMatters: "Gossip scales well and survives partial failure, which makes it attractive for large dynamic clusters.",
    relatedTerms: ["service discovery", "health checks and heartbeats", "eventual consistency", "distributed consensus"]
  },
  {
    id: "lamport-timestamps",
    term: "Lamport Timestamps",
    category: "systems",
    tags: ["distributed-systems", "ordering", "time"],
    oneLiner: "Lamport timestamps give events a logical order without relying on synchronized wall clocks.",
    body: "Each process increments a counter for local events and includes that counter in messages. When a node receives a message, it updates its own counter to be greater than both its current value and the received value. This gives a consistent happens-before ordering, but it cannot tell whether two events were truly concurrent.",
    example: "If service B receives a request from service A tagged with logical time 7, B knows its next event must be numbered at least 8.",
    whyItMatters: "Logical clocks are a basic tool for reasoning about distributed event ordering when real clock time is too unreliable to trust.",
    relatedTerms: ["vector clocks", "distributed consensus", "causality", "event ordering"]
  },
  {
    id: "vector-clocks",
    term: "Vector Clocks",
    category: "systems",
    tags: ["distributed-systems", "ordering", "conflict-resolution"],
    oneLiner: "Vector clocks track causality across multiple nodes more precisely than a single logical counter can.",
    body: "Instead of one number, a vector clock stores one counter per participant and updates the relevant entry as events happen. By comparing vectors, a system can tell whether one event happened before another or whether they are concurrent and conflicting. The extra precision costs more metadata and complexity than Lamport timestamps.",
    example: "Two replicas that both update the same record while disconnected can later detect that the writes were concurrent rather than simply newer or older.",
    whyItMatters: "Vector clocks are useful when conflict detection matters more than keeping metadata tiny, especially in multi-master or offline-first systems.",
    relatedTerms: ["lamport timestamps", "eventual consistency", "causality", "conflict resolution"]
  },
  {
    id: "two-phase-commit",
    term: "Two-Phase Commit",
    category: "systems",
    tags: ["distributed-systems", "transactions", "coordination"],
    oneLiner: "Two-phase commit is a protocol for getting multiple participants to either all commit a transaction or all abort it.",
    body: "In phase one, a coordinator asks each participant whether it can commit and waits for votes. In phase two, the coordinator tells everyone to commit if all voted yes, otherwise it tells everyone to abort. It gives atomicity across participants, but if the coordinator fails at the wrong moment the whole system can block waiting for a decision.",
    example: "A money transfer that debits one service-owned account and credits another may use two-phase commit so neither side finishes alone.",
    whyItMatters: "Two-phase commit sounds neat on paper, but its blocking behavior is why many distributed systems avoid it in favor of compensation or weaker coordination.",
    relatedTerms: ["three-phase commit", "write-ahead logging", "distributed consensus", "transaction coordinator"]
  },
  {
    id: "three-phase-commit",
    term: "Three-Phase Commit",
    category: "systems",
    tags: ["distributed-systems", "transactions", "coordination"],
    oneLiner: "Three-phase commit tries to reduce some blocking problems of two-phase commit by adding an extra coordination step.",
    body: "The protocol separates the commit decision into more stages so participants can distinguish between uncertainty and a safe-to-commit state. In theory this helps under certain failure patterns, but in practice it assumes timing conditions that real networks do not guarantee well. That is why it is discussed more in theory than used in mainstream production infrastructure.",
    example: "A participant that times out after the pre-commit stage may infer more about the transaction state than it could under classic two-phase commit.",
    whyItMatters: "Knowing three-phase commit is useful mainly because it shows how hard distributed transactions become once failures and timing uncertainty enter the picture.",
    relatedTerms: ["two-phase commit", "distributed consensus", "fault tolerance", "transaction coordinator"]
  },
  {
    id: "write-ahead-logging",
    term: "Write-Ahead Logging (WAL)",
    category: "systems",
    tags: ["storage", "databases", "durability"],
    oneLiner: "WAL means recording the intended change durably before treating the main data update as committed.",
    body: "The log becomes the source of truth for recovery after a crash, because the system can replay or roll back changes based on what was safely written first. Databases use this to make transactions durable and to recover to a consistent state after failure. It is often much cheaper to append to a log than to update many structures in place immediately.",
    example: "A database may append an update record to its WAL, flush it, acknowledge the transaction, and only later fold that change into the main data files.",
    whyItMatters: "WAL is one of the core tricks that makes crash recovery and transactional durability practical.",
    relatedTerms: ["file systems", "two-phase commit", "checkpointing", "durability"]
  },
  {
    id: "backpressure",
    term: "Backpressure",
    category: "systems",
    tags: ["distributed-systems", "streaming", "reliability"],
    oneLiner: "Backpressure is how a slower consumer tells an upstream producer to slow down before buffers explode.",
    body: "Without backpressure, fast producers can overwhelm queues, memory, sockets, or downstream services. A good system pushes overload signals backward so work is delayed, shed, or paced before the whole pipeline collapses. This can be explicit, like flow-control credits, or implicit, like rising queue depth and timeouts.",
    example: "A stream processor may stop requesting more batches from Kafka until it finishes working through the batch it already has.",
    whyItMatters: "Backpressure is one of the main differences between systems that degrade gracefully and systems that fall over in a cascade under load.",
    relatedTerms: ["thundering herd problem", "delivery guarantees", "queue depth", "rate limiting"]
  },
  {
    id: "service-discovery",
    term: "Service Discovery",
    category: "systems",
    tags: ["distributed-systems", "infrastructure", "networking"],
    oneLiner: "Service discovery is how code finds the current network location of other services without hardcoding addresses.",
    body: "In dynamic environments, instances appear, disappear, restart, and move all the time. A discovery system like Consul or etcd keeps track of which instances are healthy and where they can be reached. Clients or proxies then query that source of truth instead of relying on static host lists.",
    example: "A payments service can ask the discovery layer for healthy `inventory` instances instead of shipping a baked-in list of IP addresses.",
    whyItMatters: "Once services scale horizontally, service discovery becomes basic plumbing for routing, failover, and deploys.",
    relatedTerms: ["health checks and heartbeats", "leader election", "load balancer", "gossip protocol"]
  },
  {
    id: "health-checks-and-heartbeats",
    term: "Health Checks and Heartbeats",
    category: "systems",
    tags: ["distributed-systems", "availability", "monitoring"],
    oneLiner: "They are simple signals used to decide whether a service or node is alive enough to keep receiving work.",
    body: "A health check is usually an active probe such as an HTTP endpoint or command that asks, 'are you working right now?' A heartbeat is usually a periodic signal sent by the node itself to show it is still participating. Systems combine them to make routing and failover decisions, but must handle the fact that 'silent' does not always mean 'dead.'",
    example: "A scheduler may stop sending traffic to an instance whose readiness check fails, then trigger replacement if its heartbeats stop entirely.",
    whyItMatters: "Poor liveness signals cause either unnecessary failovers or slow detection of real failures, both of which hurt reliability.",
    relatedTerms: ["service discovery", "leader election", "observability", "gossip protocol"]
  },
  {
    id: "chaos-engineering",
    term: "Chaos Engineering",
    category: "systems",
    tags: ["reliability", "testing", "operations"],
    oneLiner: "Chaos engineering is the practice of injecting controlled failure to learn how a system behaves before production learns it for you.",
    body: "The goal is not random breakage for its own sake but disciplined experiments with clear hypotheses and rollback paths. Teams might kill instances, add latency, drop packets, or fail dependencies to see whether alerts, retries, and fallbacks behave as expected. It turns resilience from an assumption into something observed.",
    example: "You deliberately terminate a fraction of application pods during business hours to verify that autoscaling and retry behavior keep customer-facing latency stable.",
    whyItMatters: "Systems usually fail in combinations that look fine in isolated tests, and chaos work exposes those weak joints before an outage does.",
    relatedTerms: ["observability", "health checks and heartbeats", "backpressure", "capacity planning"]
  },
  {
    id: "observability",
    term: "Observability",
    category: "systems",
    tags: ["monitoring", "reliability", "operations"],
    oneLiner: "Observability is the ability to understand what a system is doing by inspecting its outputs, especially metrics, logs, and traces.",
    body: "Metrics show numeric behavior over time, logs capture discrete events and details, and traces connect work across service boundaries. Good observability is not just collecting data but making it possible to ask new questions during an incident without shipping new code first. It is what turns a black box into something you can actually debug.",
    example: "A trace can show that a slow API request spent 40 ms in auth, 180 ms waiting on a database, and 600 ms blocked on a downstream billing service.",
    whyItMatters: "When a production system misbehaves, observability determines whether you fix it in minutes or guess for hours.",
    relatedTerms: ["sli-slo-sla", "health checks and heartbeats", "chaos engineering", "capacity planning"]
  },
  {
    id: "sli-slo-sla",
    term: "SLI, SLO, and SLA",
    category: "systems",
    tags: ["reliability", "operations", "measurement"],
    oneLiner: "These are related but different ways to measure, target, and promise service behavior.",
    body: "An SLI is the thing you measure, such as request success rate or p95 latency. An SLO is the target you set for that indicator, such as 99.9 percent success over 30 days. An SLA is the external promise attached to consequences, usually in a contract, if the service misses the agreed threshold.",
    example: "A team may track a 99.95 percent availability SLI, set a 99.9 percent internal SLO, and offer a 99.5 percent customer SLA.",
    whyItMatters: "Without clear indicators and targets, reliability work drifts into vague opinions instead of deliberate tradeoffs and priorities.",
    relatedTerms: ["observability", "capacity planning", "error budget", "availability"]
  },
  {
    id: "capacity-planning",
    term: "Capacity Planning",
    category: "systems",
    tags: ["performance", "operations", "scaling"],
    oneLiner: "Capacity planning is the discipline of figuring out how much headroom a system needs before demand exposes a bottleneck.",
    body: "It combines workload assumptions, growth expectations, redundancy requirements, and failure scenarios to answer 'how much is enough?' Real planning looks beyond average traffic to peaks, maintenance windows, failover events, and noisy-neighbor effects. The goal is not maximal hardware but enough room to stay healthy when conditions stop being average.",
    example: "If one region fails and traffic shifts to the other, the surviving region must still have enough spare capacity to stay within latency targets.",
    whyItMatters: "Good capacity planning prevents both wasteful overprovisioning and embarrassing outages caused by traffic that was entirely predictable.",
    relatedTerms: ["backpressure", "sli-slo-sla", "thundering herd problem", "load testing"]
  },
  {
    id: "thundering-herd-problem",
    term: "Thundering Herd Problem",
    category: "systems",
    tags: ["performance", "distributed-systems", "reliability"],
    oneLiner: "A thundering herd happens when many workers or clients all wake up and hit the same resource at once.",
    body: "This often appears after a lock is released, a cache entry expires, or a dependency recovers from outage. Individually each request looks fine, but the synchronized burst creates a fresh overload event that can knock the system back down. Jitter, request coalescing, and staggered retries are common defenses.",
    example: "A million clients all retry at the 5-second mark after a timeout, instantly crushing the service that just started recovering.",
    whyItMatters: "A system can survive steady traffic and still fail badly under synchronized behavior, which is why herd effects show up in many real incidents.",
    relatedTerms: ["backpressure", "hot spots and data skew", "retry storm", "caching"]
  },
  {
    id: "hot-spots-and-data-skew",
    term: "Hot Spots and Data Skew",
    category: "systems",
    tags: ["distributed-systems", "scaling", "data-distribution"],
    oneLiner: "Hot spots happen when traffic or data concentrates unevenly, making one node or key far busier than the rest.",
    body: "Even a well-scaled cluster can behave like a single overloaded machine if one partition, tenant, or record receives most of the load. Data skew often comes from poor partitioning choices or naturally uneven popularity distributions. Fixes include better partition keys, adaptive routing, replication of hot reads, or explicit isolation for noisy tenants.",
    example: "If all writes for today's timestamped records land on the same partition, that shard melts while the others sit mostly idle.",
    whyItMatters: "Uneven load is one of the fastest ways to make a distributed system underperform despite having plenty of total capacity on paper.",
    relatedTerms: ["capacity planning", "sharding", "thundering herd problem", "service discovery"]
  },
  {
    id: "idempotency-keys",
    term: "Idempotency Keys",
    category: "systems",
    tags: ["distributed-systems", "apis", "reliability"],
    oneLiner: "An idempotency key lets a system treat retries of the same request as one logical operation instead of many separate ones.",
    body: "Clients attach a unique key to a request, and the server stores the first accepted result for that key. If the client retries because of a timeout or dropped connection, the server can return the original result instead of performing the side effect again. This is one of the simplest and most effective tools for safe retries in distributed systems.",
    example: "A payment API receives the same charge request twice with the same idempotency key and returns the original successful response instead of charging the card again.",
    whyItMatters: "Retries are unavoidable in real networks, and idempotency keys stop network uncertainty from turning into duplicate side effects.",
    relatedTerms: ["delivery guarantees", "backpressure", "retry", "write-ahead logging"]
  },
  {
    id: "delivery-guarantees",
    term: "Exactly-Once vs At-Least-Once Delivery",
    category: "systems",
    tags: ["distributed-systems", "messaging", "reliability"],
    oneLiner: "Delivery guarantees describe what a system promises about whether a message might be lost, duplicated, or processed more than once.",
    body: "At-least-once means messages are retried until acknowledged, so duplicates are possible but loss is reduced. Exactly-once usually means the overall system combines deduplication, transactional writes, and careful coordination so duplicates are not observed at the application boundary, not that the network magically delivered a packet once and only once. The stronger the promise, the more machinery and cost the system usually needs.",
    example: "A queue may deliver the same job twice after a worker crash, so the consumer uses an idempotency key and durable state check to avoid double-applying it.",
    whyItMatters: "A lot of reliability design comes down to deciding whether you want simpler duplicates to handle or heavier coordination to hide them.",
    relatedTerms: ["idempotency keys", "backpressure", "message queue", "write-ahead logging"]
  }
];
