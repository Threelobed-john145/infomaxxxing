import { Concept } from '@/lib/types';

export const performanceConcepts: Concept[] = [
  {
    id: "cpu-profiling",
    term: "CPU Profiling",
    category: "performance",
    tags: ["profiling", "cpu", "debugging"],
    oneLiner: "Recording where a program spends its execution time so you can fix the hottest paths first.",
    body: "CPU profiling samples or traces a running program to show which functions consume the most compute time. Instead of guessing why something feels slow, you measure the actual hotspots and inspect their call stacks. In practice, the output is often visualized as a flame graph so wide blocks immediately show where time is being burned.",
    example: "A page feels sluggish during search. A CPU profile shows most of the time is spent repeatedly sorting the same array on every keystroke.",
    whyItMatters: "Performance work goes sideways when it starts with hunches. CPU profiling gives you evidence, so you can spend effort on the code that actually moves the needle.",
    relatedTerms: ["flame graphs", "benchmarking", "hot path", "sampling profiler"]
  },
  {
    id: "memory-profiling",
    term: "Memory Profiling",
    category: "performance",
    tags: ["profiling", "memory", "debugging"],
    oneLiner: "Inspecting how much memory your program uses, what is allocating it, and what never gets released.",
    body: "Memory profiling shows which objects are being created, retained, and promoted over time. It helps separate healthy short-lived allocations from suspicious growth that keeps the heap expanding. Common tools include allocation timelines, heap snapshots, and retained-size views that reveal what is keeping memory alive.",
    example: "A worker process starts at 200 MB and slowly grows to 1.5 GB. Memory profiling shows a cache map holding onto old entries long after requests finish.",
    whyItMatters: "Slow memory growth often turns into crashes, long pauses, and expensive infrastructure bills. Memory profiling lets you find the specific structures behind the growth instead of treating it as a mystery.",
    relatedTerms: ["heap snapshot", "memory leaks", "garbage collection", "retained size"]
  },
  {
    id: "memory-leaks",
    term: "Memory Leaks",
    category: "performance",
    tags: ["memory", "reliability", "debugging"],
    oneLiner: "A memory leak happens when code keeps references to data that should have been cleaned up.",
    body: "A leak is not just high memory use - it is memory that stays reachable even though the program no longer needs it. Typical causes include forgotten event listeners, global caches with no eviction, closures that capture large objects, and detached DOM nodes. You usually detect leaks by comparing heap snapshots before and after repeated actions and looking for object counts that never return to baseline.",
    example: "Opening and closing a modal 50 times should leave memory roughly flat. If each open adds listeners and DOM nodes that never disappear, the heap keeps climbing after every cycle.",
    whyItMatters: "Leaks make systems slower the longer they run, and they often surface under real traffic instead of local happy paths. Finding them early prevents restarts, instability, and hard-to-reproduce production failures.",
    relatedTerms: ["memory profiling", "heap snapshot", "garbage collection", "retention path"]
  },
  {
    id: "garbage-collection-tuning",
    term: "Garbage Collection Tuning",
    category: "performance",
    tags: ["runtime", "memory", "latency"],
    oneLiner: "Adjusting allocation patterns or runtime settings so garbage collection does less disruptive work.",
    body: "Garbage collectors reclaim memory automatically, but that work is not free. If your app creates too many temporary objects or keeps the heap too full, collection pauses can become frequent or long. Tuning usually means reducing allocation churn first, then adjusting heap sizes or collector settings only when measurement shows it helps.",
    example: "A service with spiky p99 latency turns out to spend large chunks of time in stop-the-world collection because each request builds thousands of throwaway objects.",
    whyItMatters: "A system can look fast on average while still feeling terrible because GC pauses hit users at the wrong moments. Tuning keeps latency more predictable and prevents memory pressure from becoming a hidden bottleneck.",
    relatedTerms: ["memory profiling", "heap pressure", "allocation rate", "tail latency"]
  },
  {
    id: "object-pooling",
    term: "Object Pooling",
    category: "performance",
    tags: ["memory", "allocation", "optimization"],
    oneLiner: "Reusing expensive objects instead of constantly allocating and destroying them.",
    body: "Object pooling keeps a reusable set of objects ready for work so the program can avoid repeated setup and teardown costs. It is most useful when objects are expensive to create or when allocation churn is causing measurable GC pressure. Used carelessly, though, pools can retain memory longer than necessary and make code harder to reason about.",
    example: "A game engine recycles particle objects instead of allocating thousands of new ones every frame during an explosion.",
    whyItMatters: "Pooling can smooth out latency and reduce pressure on the allocator in tight loops. The important part is to use it only when profiling shows allocation overhead is a real problem.",
    relatedTerms: ["garbage collection", "allocation rate", "resource reuse", "memory profiling"]
  },
  {
    id: "code-splitting",
    term: "Code Splitting",
    category: "performance",
    tags: ["frontend", "bundles", "loading"],
    oneLiner: "Breaking a large JavaScript bundle into smaller pieces that load only when needed.",
    body: "Instead of shipping the entire app upfront, code splitting lets the browser fetch route-level or feature-level chunks on demand. That lowers the amount of JavaScript needed for the first render and shortens parse and execution time. The tradeoff is that poorly planned splits can create too many network requests or delay later interactions.",
    example: "An admin dashboard is loaded only when a user visits `/admin`, rather than being included in the landing page bundle for everyone.",
    whyItMatters: "JavaScript cost is not just download size - it is also parse, compile, and execution time on real devices. Code splitting keeps the initial experience lighter and makes large apps feel less heavy.",
    relatedTerms: ["tree shaking", "bundle size", "lazy loading", "critical rendering path"]
  },
  {
    id: "tree-shaking",
    term: "Tree Shaking",
    category: "performance",
    tags: ["frontend", "bundles", "build-tools"],
    oneLiner: "Removing code from the final bundle that is never actually used.",
    body: "Modern bundlers analyze imports and exports to drop dead code from production builds. This works best with modules that are statically analyzable and have minimal side effects. If a library ships as one big side-effect-heavy package, tree shaking often cannot remove much from it.",
    example: "Importing one utility from a well-structured package adds a few hundred bytes, while importing from a legacy all-in-one package drags in the whole library.",
    whyItMatters: "Unused code still costs bandwidth, parse time, and memory. Tree shaking keeps bundles lean without changing what users can do.",
    relatedTerms: ["code splitting", "bundle size", "dead code elimination", "side effects"]
  },
  {
    id: "bundle-size-budgeting",
    term: "Bundle Size Budgeting",
    category: "performance",
    tags: ["frontend", "bundles", "governance"],
    oneLiner: "Setting explicit size limits so performance does not quietly regress over time.",
    body: "Bundle size optimization is easier when you treat size as a budget, not a surprise. Teams track the weight of scripts, styles, and critical assets, then fail builds or raise alerts when changes exceed agreed thresholds. That forces tradeoffs into the open before another dependency quietly adds hundreds of kilobytes.",
    example: "A pull request adds a charting library that increases the main bundle by 180 KB. The budget check catches it, so the team swaps in a lighter option.",
    whyItMatters: "Performance usually gets worse through accumulation, not one dramatic mistake. Budgets create a guardrail that keeps small regressions from becoming a large one.",
    relatedTerms: ["bundle size", "tree shaking", "code splitting", "performance budget"]
  },
  {
    id: "image-optimization",
    term: "Image Optimization",
    category: "performance",
    tags: ["images", "frontend", "web"],
    oneLiner: "Serving the smallest image that still looks good on the current device and network.",
    body: "Good image optimization combines modern formats like WebP or AVIF, responsive `srcset` sizing, compression, and delayed loading for offscreen assets. The goal is to avoid sending a huge desktop image to a small mobile screen or shipping a heavier format than necessary. Images often dominate page weight, so even basic fixes can produce large wins.",
    example: "A hero image that used to ship as a 1.2 MB JPEG becomes a 220 KB AVIF on modern browsers, with a fallback format for older ones and smaller variants for phones.",
    whyItMatters: "Image bytes are some of the easiest performance waste to spot and some of the highest impact to remove. Better image delivery improves loading speed without changing product behavior.",
    relatedTerms: ["responsive images", "lazy loading", "largest contentful paint", "compression"]
  },
  {
    id: "critical-rendering-path",
    term: "Critical Rendering Path",
    category: "performance",
    tags: ["rendering", "frontend", "web"],
    oneLiner: "The sequence of work the browser must finish before users can see useful content.",
    body: "The browser has to fetch HTML, discover resources, build the DOM and CSSOM, and paint the page before anything appears. Render-blocking CSS, synchronous scripts, and slow font loading can all stretch that path. Optimizing the critical rendering path means reducing blocking work so the first visible content appears sooner.",
    example: "Inlining a small amount of critical CSS and deferring non-essential scripts lets the page paint the header immediately instead of waiting for the whole app bundle.",
    whyItMatters: "Users judge speed by when they can see and use something, not by when every asset is done. Tightening the critical path improves perceived performance and core web vitals.",
    relatedTerms: ["first contentful paint", "resource hints", "render blocking", "time to first byte"]
  },
  {
    id: "time-to-first-byte",
    term: "Time to First Byte (TTFB)",
    category: "performance",
    tags: ["web", "backend", "metrics"],
    oneLiner: "The time between making a request and receiving the first byte of the server's response.",
    body: "TTFB captures everything that happens before the response starts: DNS, connection setup, TLS, edge routing, queueing, and server processing. A poor TTFB can come from slow application logic, cache misses, cold starts, or simply serving users from infrastructure that is too far away. It does not describe the whole page experience, but it strongly affects how quickly rendering can begin.",
    example: "A homepage with 900 ms TTFB gets faster after moving cacheable HTML to the edge and reducing a database query on the request path.",
    whyItMatters: "If the first byte is late, every later milestone is pushed back. TTFB is often the earliest signal that server-side work or network setup is delaying the entire experience.",
    relatedTerms: ["critical rendering path", "cdn optimization", "caching strategies", "latency"]
  },
  {
    id: "paint-metrics",
    term: "Paint Metrics (FCP and LCP)",
    category: "performance",
    tags: ["web-vitals", "rendering", "metrics"],
    oneLiner: "FCP measures when something first appears, and LCP measures when the main visible content shows up.",
    body: "First Contentful Paint marks the first time the browser renders text, an image, or another visible element. Largest Contentful Paint focuses on the biggest meaningful element in the viewport, such as a hero image or page headline. Together they tell you whether users see something quickly and whether the page's main content arrives without lagging behind.",
    example: "A page can have a decent FCP because a small header appears fast, yet still have poor LCP because the main article image loads much later.",
    whyItMatters: "Fast first paint is not enough if the important content still drags. These metrics help teams distinguish a quick-looking shell from a genuinely fast page.",
    relatedTerms: ["largest contentful paint", "first contentful paint", "critical rendering path", "image optimization"]
  },
  {
    id: "cumulative-layout-shift",
    term: "Cumulative Layout Shift (CLS)",
    category: "performance",
    tags: ["web-vitals", "ux", "layout"],
    oneLiner: "A measure of how much page content jumps around while the page is loading or updating.",
    body: "Layout shift happens when visible elements move unexpectedly after they were already drawn. Common causes include images without dimensions, ads that resize after load, and fonts that swap into different metrics. CLS tracks the visual instability those shifts create, which users experience as a page that feels slippery or broken.",
    example: "You try to tap a button, but an ad loads above it and pushes everything down right before the click lands.",
    whyItMatters: "A page can be technically fast and still feel bad if the interface keeps moving under the user. Reducing CLS improves trust, usability, and conversion-sensitive flows.",
    relatedTerms: ["web vitals", "layout stability", "responsive images", "font loading"]
  },
  {
    id: "interaction-to-next-paint",
    term: "Interaction to Next Paint (INP)",
    category: "performance",
    tags: ["web-vitals", "interactivity", "latency"],
    oneLiner: "A measure of how quickly the interface visibly responds after a user interacts with it.",
    body: "INP looks at the delay between an interaction, the processing work it triggers, and the next painted frame. Heavy JavaScript, long tasks, and main-thread contention are common reasons it gets worse. Unlike narrow click-delay metrics, INP reflects the end-to-end responsiveness users actually feel.",
    example: "Clicking a filter button appears to do nothing for 400 ms because the main thread is busy recalculating a large list before the browser can paint the updated state.",
    whyItMatters: "People forgive a slightly slower page load more easily than a page that ignores them after input. INP is a strong signal for whether an app feels snappy in real use.",
    relatedTerms: ["long tasks", "requestAnimationFrame", "web workers", "main thread"]
  },
  {
    id: "list-virtualization",
    term: "List Virtualization",
    category: "performance",
    tags: ["frontend", "rendering", "ui"],
    oneLiner: "Rendering only the visible slice of a long list instead of mounting every item at once.",
    body: "Virtualization, also called windowing, keeps the DOM small by drawing only the rows currently on screen plus a small buffer. As the user scrolls, old rows are recycled and new ones are rendered into view. This avoids the cost of laying out and updating thousands of invisible elements.",
    example: "A table with 100,000 rows stays smooth because the browser only holds about 30 visible rows in the DOM at any moment.",
    whyItMatters: "Large lists can wreck scrolling performance even when the data itself is cheap to fetch. Virtualization protects both memory use and frame rate in data-heavy interfaces.",
    relatedTerms: ["windowing", "infinite scroll", "rendering performance", "requestAnimationFrame"]
  },
  {
    id: "web-workers",
    term: "Web Workers",
    category: "performance",
    tags: ["frontend", "concurrency", "browser"],
    oneLiner: "Running CPU-heavy work off the main browser thread so the UI can stay responsive.",
    body: "The browser main thread is shared by rendering, input handling, and most application JavaScript. Web Workers let you move heavy computation, parsing, or data transformation into a background thread and pass messages back when the work is done. The tradeoff is that worker communication has overhead and shared state is limited.",
    example: "A spreadsheet app moves formula recalculation into a worker so typing in a cell does not freeze the entire page.",
    whyItMatters: "If all expensive work stays on the main thread, users pay for it with jank and delayed input. Workers are one of the clearest ways to protect interactivity during CPU-intensive tasks.",
    relatedTerms: ["interaction to next paint", "main thread", "message passing", "long tasks"]
  },
  {
    id: "request-animation-frame",
    term: "requestAnimationFrame (rAF)",
    category: "performance",
    tags: ["animation", "browser", "rendering"],
    oneLiner: "A browser API for scheduling visual updates to line up with the next paint.",
    body: "requestAnimationFrame tells the browser, 'run this right before you render the next frame.' That makes animations and scroll-linked updates smoother than using fixed timers like `setInterval`, which can drift or fire at awkward times. rAF also pauses in background tabs, which avoids wasted work.",
    example: "Dragging a slider updates its position in a requestAnimationFrame loop, keeping motion aligned with the display refresh instead of stuttering under timer jitter.",
    whyItMatters: "Smooth motion depends on doing the right amount of work at the right time. rAF is the basic tool for syncing visual updates with the browser's rendering pipeline.",
    relatedTerms: ["animation frame", "interaction to next paint", "gpu acceleration", "main thread"]
  },
  {
    id: "gpu-acceleration",
    term: "GPU Acceleration",
    category: "performance",
    tags: ["rendering", "graphics", "browser"],
    oneLiner: "Using the graphics pipeline to make certain visual changes cheaper and smoother.",
    body: "Some properties, like `transform` and `opacity`, can often be updated in compositor layers without forcing expensive layout or paint work. Hints such as `will-change` can help the browser prepare for upcoming animation, but overusing them can waste memory and create too many layers. The goal is not 'put everything on the GPU' - it is to choose the right properties and layers for the effect you need.",
    example: "Animating a card with `transform: translateY(...)` stays smoother than animating `top`, which can force layout recalculation on every frame.",
    whyItMatters: "A small CSS choice can decide whether an animation feels crisp or janky. Understanding compositing helps you build motion that looks good without burning through frame budget.",
    relatedTerms: ["compositing layers", "requestAnimationFrame", "layout thrashing", "will-change"]
  },
  {
    id: "database-query-optimization",
    term: "Database Query Optimization",
    category: "performance",
    tags: ["databases", "backend", "queries"],
    oneLiner: "Improving query speed by using better indexes, better plans, and fewer unnecessary round trips.",
    body: "Query optimization starts by checking how the database actually executes a query, not by assuming the SQL is fine. Indexes can eliminate costly scans, query plans reveal where time is really going, and the N+1 pattern shows when an app is issuing far too many small queries. The best fixes often combine schema changes, SQL changes, and application-level batching.",
    example: "A page that loads 50 posts and then fetches each author separately runs 51 queries. Rewriting it as a join or batched lookup drops both latency and database load.",
    whyItMatters: "Databases are frequently the slowest part of a request path, and inefficient queries get more painful as traffic grows. This is one of the highest-leverage places to improve backend performance.",
    relatedTerms: ["indexes", "query plan", "n+1 query problem", "connection pooling"]
  },
  {
    id: "caching-strategies",
    term: "Caching Strategies",
    category: "performance",
    tags: ["caching", "backend", "architecture"],
    oneLiner: "Choosing how and when data should be read from cache versus source of truth.",
    body: "Cache-aside loads data into cache on demand after a miss, write-through updates cache at the same time as the database, and write-behind updates cache first and persists later. Each strategy trades off freshness, simplicity, and failure behavior differently. The right choice depends on whether you care most about read speed, consistency, or write throughput.",
    example: "A product catalog uses cache-aside because stale data for a few minutes is acceptable and reads greatly outnumber writes.",
    whyItMatters: "Caching can remove huge amounts of repeated work, but a bad strategy can create stale data or hard-to-debug edge cases. Knowing the common patterns helps you pick the least risky speedup.",
    relatedTerms: ["cache-aside", "write-through", "write-behind", "cdn optimization"]
  },
  {
    id: "cdn-optimization",
    term: "CDN Optimization",
    category: "performance",
    tags: ["networking", "caching", "edge"],
    oneLiner: "Using edge locations and cache behavior wisely so content reaches users faster.",
    body: "A CDN helps by serving cacheable content from locations closer to users, but results depend on cache keys, TTLs, compression, and origin behavior. If headers are wrong, a CDN becomes a pass-through instead of a speedup. Good CDN optimization also includes deciding which responses belong at the edge and which should always hit origin.",
    example: "Static assets and cacheable HTML are served from edge nodes around the world, cutting response time for distant users without changing the app itself.",
    whyItMatters: "Distance and repeated origin work are both expensive. A well-configured CDN reduces both, which improves speed and lowers load on the main servers.",
    relatedTerms: ["time to first byte", "edge caching", "resource hints", "http compression"]
  },
  {
    id: "http-compression",
    term: "HTTP Compression",
    category: "performance",
    tags: ["networking", "compression", "web"],
    oneLiner: "Shrinking responses before sending them over the network to reduce transfer time.",
    body: "Text-heavy assets like HTML, CSS, JavaScript, JSON, and SVG usually compress well with algorithms such as gzip, Brotli, or zstd. Better compression ratios reduce bandwidth, but stronger compression can cost more CPU at the server. The practical goal is to pick defaults that lower transfer size without turning compression itself into a bottleneck.",
    example: "A 300 KB JavaScript file might shrink to under 100 KB with Brotli, which cuts download time significantly on slower networks.",
    whyItMatters: "Many performance problems are still just too many bytes crossing the wire. Compression is one of the simplest ways to reduce that cost across almost every request.",
    relatedTerms: ["brotli", "gzip", "zstd", "bundle size"]
  },
  {
    id: "http2-multiplexing-server-push",
    term: "HTTP/2 Multiplexing and Server Push",
    category: "performance",
    tags: ["networking", "protocols", "web"],
    oneLiner: "HTTP/2 lets many requests share one connection, and server push tried to send likely-needed assets before the browser asked.",
    body: "Multiplexing avoids the head-of-line pain of opening many separate connections by letting multiple streams move over one TCP connection. Server push was designed to proactively deliver assets, but it could also waste bandwidth when the browser already had them or did not need them. The useful lesson is that protocol features can help, but only when they match real loading behavior.",
    example: "A page loads CSS, JavaScript, and API requests concurrently over one connection instead of waiting on a chain of separate downloads.",
    whyItMatters: "Transport details affect how quickly assets arrive and how much connection overhead users pay. Understanding HTTP/2 helps you reason about why some loading strategies work better than others.",
    relatedTerms: ["multiplexing", "resource hints", "time to first byte", "cdn optimization"]
  },
  {
    id: "resource-hints-dns-prefetch",
    term: "Resource Hints and DNS Prefetch",
    category: "performance",
    tags: ["frontend", "networking", "loading"],
    oneLiner: "Telling the browser which connections or files are likely to matter soon so it can prepare earlier.",
    body: "Resource hints include `preload`, `prefetch`, `preconnect`, and `dns-prefetch`, each with a different level of urgency and cost. `dns-prefetch` only resolves the domain name, while `preconnect` goes further by opening the connection, and `preload` asks for a specific high-priority asset now. Used well, hints shorten waiting time; used badly, they compete with truly critical resources.",
    example: "A checkout page preconnects to the payment provider and preloads its main font so those steps are already underway when the browser needs them.",
    whyItMatters: "Browsers are fast, but they can only optimize around what they know. Resource hints let you expose likely future work early enough to cut avoidable delay.",
    relatedTerms: ["preload", "prefetch", "preconnect", "critical rendering path"]
  },
  {
    id: "tail-latency",
    term: "Tail Latency",
    category: "performance",
    tags: ["latency", "metrics", "distributed-systems"],
    oneLiner: "The slowest slice of requests, often described with percentiles like p99 or p999.",
    body: "Averages can hide the user experience because a small number of very slow requests barely move the mean. Tail latency focuses on the worst-performing percentiles, where queueing, lock contention, cache misses, retries, and noisy neighbors tend to show up. In distributed systems, the tail matters even more because one slow dependency can hold up the whole request.",
    example: "An API with a 70 ms average sounds healthy until you notice its p99 is 2.4 seconds during traffic spikes.",
    whyItMatters: "Users remember the bad outliers more than the average. Measuring the tail helps you find instability that summary statistics can easily hide.",
    relatedTerms: ["p99", "little's law", "queueing", "garbage collection tuning"]
  },
  {
    id: "load-testing",
    term: "Load Testing",
    category: "performance",
    tags: ["testing", "capacity", "reliability"],
    oneLiner: "Simulating realistic traffic to find the point where a system slows down or breaks.",
    body: "Load testing tools such as k6, Artillery, and Locust generate concurrent traffic so you can observe throughput, latency, error rate, and resource use under pressure. The goal is not just to see whether the system survives, but to identify the first bottleneck and the conditions that trigger it. Good load tests model realistic request mixes and think times instead of using one synthetic endpoint forever.",
    example: "A service handles 200 requests per second comfortably, but latency spikes sharply at 350 because the database pool saturates.",
    whyItMatters: "Many systems look fine until concurrency rises. Load testing exposes scaling limits before users do, which makes capacity planning much less guessy.",
    relatedTerms: ["stress testing", "benchmarking", "tail latency", "throughput"]
  },
  {
    id: "benchmarking",
    term: "Benchmarking Best Practices",
    category: "performance",
    tags: ["measurement", "testing", "methodology"],
    oneLiner: "Running repeatable performance tests that measure the thing you actually care about.",
    body: "A good benchmark controls noise, warms up the system when needed, uses representative inputs, and reports enough runs to show variation rather than one lucky number. It also isolates the unit under test so setup cost, network jitter, or unrelated background work do not dominate the result. The hardest part is making sure the benchmark matches real workload shape instead of a toy case.",
    example: "Comparing two JSON parsers on a tiny static file says little about production if real requests use larger, messier payloads and different allocation patterns.",
    whyItMatters: "Bad benchmarks create fake confidence and drive bad decisions. Good benchmarks give you evidence you can trust when choosing between two implementations or tuning a hot path.",
    relatedTerms: ["cpu profiling", "load testing", "microbenchmark", "variance"]
  },
  {
    id: "amdahls-law",
    term: "Amdahl's Law",
    category: "performance",
    tags: ["theory", "scaling", "optimization"],
    oneLiner: "The overall speedup is limited by the part of the system you did not improve.",
    body: "If only 20 percent of a program can benefit from your optimization, making that part infinitely fast still leaves the other 80 percent untouched. Amdahl's Law is a reminder that local wins have a hard ceiling when the rest of the workflow stays the same. It pushes you to look at total time composition before investing in heroics.",
    example: "Making image decoding twice as fast barely changes page load time if network transfer is still the dominant cost.",
    whyItMatters: "This law keeps optimization grounded in system-level reality. It helps teams avoid spending weeks polishing a small slice that can only deliver a tiny overall gain.",
    relatedTerms: ["bottleneck", "profiling", "critical path", "premature optimization"]
  },
  {
    id: "littles-law",
    term: "Little's Law",
    category: "performance",
    tags: ["queueing", "systems", "theory"],
    oneLiner: "Average items in a system equals arrival rate multiplied by average time spent in the system.",
    body: "Little's Law is usually written as L = lambda * W, where L is work in progress, lambda is throughput, and W is response time. It gives a simple way to reason about why queues grow when arrival rate rises or service time increases. Even without perfect precision, it is a powerful mental model for understanding congestion in servers, worker queues, and pipelines.",
    example: "If a queue receives 100 jobs per second and each job spends 0.5 seconds in the system on average, you should expect about 50 jobs in flight.",
    whyItMatters: "It connects throughput, latency, and concurrency in one relationship. That makes it useful for planning worker counts, diagnosing queue buildup, and understanding why small slowdowns can create large backlogs.",
    relatedTerms: ["queueing theory", "tail latency", "throughput", "concurrency"]
  },
  {
    id: "premature-optimization",
    term: "Premature Optimization",
    category: "performance",
    tags: ["tradeoffs", "engineering", "decision-making"],
    oneLiner: "Optimizing too early often adds complexity before you know whether the problem is real.",
    body: "Performance work has a cost: more code, more constraints, and more chances to make behavior harder to understand. Premature optimization happens when teams add that cost before measuring a real bottleneck or before scale justifies it. The right move is usually to build clearly, measure honestly, and optimize the parts that prove they matter.",
    example: "A team builds a complicated caching layer for a feature with tiny traffic, while the real slowdown comes from one slow SQL query elsewhere.",
    whyItMatters: "Not every inefficiency deserves immediate action. This concept protects code quality and team time by keeping optimization tied to evidence and user impact.",
    relatedTerms: ["benchmarking", "cpu profiling", "amdahl's law", "tradeoffs"]
  }
];
